# ✅ Проверка работы сайта и загрузки товаров из базы

## 🔍 Шаг 1: Проверка что сайт работает

### Проверка через браузер:
1. Откройте **https://cloudcitylife.com**
2. Сайт должен открыться без ошибок

### Проверка через curl:
```bash
curl -I https://cloudcitylife.com
# Должен вернуть: HTTP/2 200
```

## 🔍 Шаг 2: Проверка что товары загружаются из базы

### Способ 1: Проверка через страницы сайта

1. **Откройте страницу с товарами:**
   - https://cloudcitylife.com/Services
   - https://cloudcitylife.com/Servers
   - https://cloudcitylife.com/VPS
   - https://cloudcitylife.com/VPN

2. **Проверьте что отображаются товары:**
   - Должны быть видны карточки товаров
   - Должны быть цены, описания, характеристики

### Способ 2: Проверка через API/прямой запрос

```bash
# Проверка через curl
curl https://cloudcitylife.com/Services
# Должен вернуть HTML с товарами

# Или проверьте JSON (если есть API)
curl https://cloudcitylife.com/api/products
```

### Способ 3: Проверка в базе данных

```sql
USE CloudCityDB;

-- Проверка количества товаров
SELECT COUNT(*) as TotalProducts FROM Products;
-- Должно быть: 16

-- Проверка конкретных товаров
SELECT Id, Name, Type, PricePerMonth, Location 
FROM Products 
ORDER BY Type, Name;

-- Проверка вариантов
SELECT p.Name as ProductName, pv.Name as VariantName, pv.Price
FROM Products p
INNER JOIN ProductVariants pv ON p.Id = pv.ProductId
ORDER BY p.Name;

-- Проверка характеристик
SELECT p.Name as ProductName, pf.Name as FeatureName, pf.Value
FROM Products p
INNER JOIN ProductFeatures pf ON p.Id = pf.ProductId
WHERE p.Name = 'DS1-E3'
ORDER BY pf.Name;
```

## 🔍 Шаг 3: Изменение данных в базе для проверки

Чтобы убедиться что данные точно из базы, измените что-то в SQL:

```sql
USE CloudCityDB;

-- Измените цену товара
UPDATE Products 
SET PricePerMonth = 999.99 
WHERE Name = 'DS1-E3';

-- Или измените название
UPDATE Products 
SET Name = 'TEST PRODUCT' 
WHERE Slug = 'ds1-e3';
```

Затем:
1. Обновите страницу сайта (Ctrl+F5 для полной перезагрузки)
2. Проверьте что изменения отобразились на сайте

**ВАЖНО:** После проверки верните данные обратно!

```sql
-- Верните обратно
UPDATE Products 
SET PricePerMonth = 59.00, Name = 'DS1-E3'
WHERE Slug = 'ds1-e3';
```

## 🔍 Шаг 4: Проверка логов приложения

На сервере проверьте логи:

```bash
# Если используете systemd
sudo journalctl -u cloudcity -f

# Или если запущено вручную
tail -f /home/siteadmin/cloudcity/CloudCityCenter/app.log
```

В логах должны быть запросы к базе данных:
```
info: Microsoft.EntityFrameworkCore.Database.Command[20101]
      Executing DbCommand...
      SELECT ...
      FROM [Products] AS [p]
```

## 🔍 Шаг 5: Проверка подключения к базе

Убедитесь что приложение использует правильную базу:

```bash
# Проверьте переменные окружения
sudo systemctl show cloudcity | grep Environment
# Должно быть:
# Environment=ASPNETCORE_ENVIRONMENT=Production
# Environment=ConnectionStrings__DefaultConnection=Server=10.151.10.8...
```

## ✅ Что должно быть видно на сайте

1. **Главная страница** - должна загружаться
2. **Страница Services** - должна показывать товары (серверы, VPS, VPN)
3. **Страница Servers** - должна показывать Dedicated Servers
4. **Страница VPS** - должна показывать VPS серверы
5. **Страница VPN** - должна показывать VPN услуги

## 🐛 Если товары не отображаются

1. **Проверьте логи приложения** на ошибки
2. **Проверьте подключение к базе:**
   ```sql
   -- В SQL Server проверьте что данные есть
   SELECT COUNT(*) FROM Products;
   ```
3. **Проверьте что приложение использует Production окружение:**
   ```bash
   sudo journalctl -u cloudcity | grep "Hosting environment"
   # Должно быть: Production
   ```
4. **Проверьте строку подключения** в логах (может быть замаскирована)

## 📝 Быстрая проверка

Выполните на сервере:

```bash
# 1. Проверка что приложение работает
curl -I http://localhost:5000

# 2. Проверка что товары в базе
# (выполните в SQL Server Management Studio)
# SELECT COUNT(*) FROM Products; -- должно быть 16

# 3. Проверка что сайт доступен
curl -I https://cloudcitylife.com
```

Если все три проверки прошли успешно - всё работает! ✅













