# 🔍 Диагностика блокировки SMTP портов

## Проблема

```
Trying 172.65.255.143...
```

Подключение зависает - это означает блокировку на уровне:
- Файрвола сервера (UFW, iptables)
- Провайдера/VPS (блокировка исходящих SMTP портов)
- Сетевых настроек

## Быстрая диагностика

### 1. Проверьте файрвол UFW:

```bash
# Проверьте статус
sudo ufw status verbose

# Если активен, разрешите порты
sudo ufw allow out 587/tcp
sudo ufw allow out 465/tcp
sudo ufw reload

# Или временно отключите для теста
sudo ufw disable
# Попробуйте подключение
timeout 10 telnet smtp.hostinger.com 587
# Если заработало - проблема в UFW
sudo ufw enable
sudo ufw allow out 587/tcp
sudo ufw allow out 465/tcp
```

### 2. Проверьте iptables:

```bash
# Посмотрите правила
sudo iptables -L OUTPUT -n -v

# Если есть блокировки, разрешите
sudo iptables -A OUTPUT -p tcp --dport 587 -j ACCEPT
sudo iptables -A OUTPUT -p tcp --dport 465 -j ACCEPT

# Сохраните правила (если нужно)
sudo iptables-save | sudo tee /etc/iptables/rules.v4
```

### 3. Используйте скрипт проверки:

```bash
chmod +x БЫСТРАЯ_ПРОВЕРКА_ФАЙРВОЛА.sh
sudo ./БЫСТРАЯ_ПРОВЕРКА_ФАЙРВОЛА.sh
```

## Если файрвол не помогает

Скорее всего, **провайдер/VPS блокирует исходящие SMTP порты**. Это распространенная практика для предотвращения спама.

### Решения:

1. **Свяжитесь с поддержкой провайдера:**
   - Объясните, что нужно отправлять email через SMTP
   - Попросите разрешить исходящие подключения на порты 587 и 465
   - Укажите IP адрес вашего сервера

2. **Используйте Web3Forms** (рекомендуется):
   - Уже настроено в коде
   - Не требует SMTP
   - Работает через HTTPS (порт 443, всегда открыт)
   - Получите ключ на https://web3forms.com

3. **Используйте другой SMTP сервис:**
   - SendGrid (API, не требует SMTP)
   - Mailgun (API или SMTP на других портах)

## Проверка после изменений

После разрешения портов в файрволе:

```bash
# Тест порта 587
timeout 10 nc -zv smtp.hostinger.com 587

# Тест порта 465
timeout 10 nc -zv smtp.hostinger.com 465

# Если видите "succeeded" - порты открыты!
```

---

**Скорее всего проблема в файрволе. Проверьте UFW первым делом!**


