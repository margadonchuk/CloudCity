# ✅ Быстрая проверка работы сайта

## 🎯 Проверка что товары из базы данных

### Способ 1: Проверка через сайт (самый простой)

1. **Откройте в браузере:**
   - https://cloudcitylife.com/Services - должны быть видны все услуги
   - https://cloudcitylife.com/Servers - должны быть 6 Dedicated Servers
   - https://cloudcitylife.com/VPS - должны быть 6 VPS серверов
   - https://cloudcitylife.com/VPN - должны быть 2 VPN услуги

2. **Проверьте что товары отображаются:**
   - Названия товаров (DS1-E3, VPS1-1-20, и т.д.)
   - Цены (€59, €10, и т.д.)
   - Характеристики (CPU, RAM, Storage)

### Способ 2: Тест изменения данных (100% гарантия)

**Этот способ точно покажет что данные из базы!**

1. **Измените цену товара в SQL Server:**
   ```sql
   USE CloudCityDB;
   
   -- Запомните текущую цену
   SELECT Name, PricePerMonth FROM Products WHERE Name = 'DS1-E3';
   
   -- Измените цену на тестовую
   UPDATE Products 
   SET PricePerMonth = 999.99 
   WHERE Name = 'DS1-E3';
   ```

2. **Обновите страницу сайта:**
   - Откройте https://cloudcitylife.com/Servers
   - Нажмите Ctrl+F5 (полная перезагрузка)
   - **Цена DS1-E3 должна быть €999.99!**

3. **Верните цену обратно:**
   ```sql
   UPDATE Products 
   SET PricePerMonth = 59.00 
   WHERE Name = 'DS1-E3';
   ```

Если цена изменилась на сайте - **100% данные из базы!** ✅

### Способ 3: Проверка через SQL запрос

```sql
USE CloudCityDB;

-- Проверка что товары есть
SELECT 
    Type,
    COUNT(*) as Count,
    STRING_AGG(Name, ', ') as Products
FROM Products
WHERE IsPublished = 1
GROUP BY Type;
```

**Ожидаемый результат:**
- DedicatedServer: 6 товаров
- VPS: 6 товаров  
- VPN: 2 услуги
- Hosting: 1 услуга
- Website: 1 услуга

### Способ 4: Проверка логов приложения

На сервере выполните:

```bash
# Смотрите логи в реальном времени
sudo journalctl -u cloudcity -f

# Или если запущено вручную
tail -f /home/siteadmin/cloudcity/CloudCityCenter/app.log
```

Затем откройте страницу https://cloudcitylife.com/Services в браузере.

В логах должны появиться SQL запросы:
```
info: Microsoft.EntityFrameworkCore.Database.Command[20101]
      Executing DbCommand...
      SELECT [p].[Id], [p].[Name], [p].[Slug]...
      FROM [Products] AS [p]
      WHERE ([p].[Type] IN (0, 3, 4) AND [p].[IsPublished] = CAST(1 AS bit))
```

Если видите такие запросы - **данные точно из базы!** ✅

## 📋 Чек-лист проверки

- [ ] Сайт открывается: https://cloudcitylife.com
- [ ] Страница Services показывает товары
- [ ] Страница Servers показывает 6 серверов
- [ ] Страница VPS показывает 6 VPS
- [ ] Страница VPN показывает 2 услуги
- [ ] Цены отображаются правильно
- [ ] Характеристики отображаются
- [ ] Изменение в SQL отражается на сайте (тест)

## 🐛 Если товары не отображаются

1. **Проверьте что товары в базе:**
   ```sql
   SELECT COUNT(*) FROM Products WHERE IsPublished = 1;
   -- Должно быть 16
   ```

2. **Проверьте что приложение использует Production:**
   ```bash
   sudo journalctl -u cloudcity | grep "Hosting environment"
   # Должно быть: Production
   ```

3. **Проверьте подключение к базе в логах:**
   ```bash
   sudo journalctl -u cloudcity | grep -i "connection\|database\|sql"
   ```

4. **Перезапустите приложение:**
   ```bash
   sudo systemctl restart cloudcity
   ```

## ✅ Итог

Если все проверки пройдены - **товары точно загружаются из SQL Server базы данных!** 🎉













