# 🔍 Диагностика проблемы с Hostinger SMTP

## Симптомы

- ✅ Пакеты проходят через NAT (порт 465 виден в логах)
- ❌ Письма не доходят на почту
- ❌ Возможно, Hostinger блокирует внешние подключения

## Возможные причины

### 1. Hostinger требует разрешения внешних SMTP подключений

Некоторые хостинг-провайдеры по умолчанию блокируют внешние SMTP подключения для безопасности. Нужно разрешить это в панели управления.

**Решение:**
1. Зайдите в панель управления Hostinger
2. Найдите настройки почты для домена `cloudcity.center`
3. Включите "Разрешить внешние SMTP подключения" или "Enable SMTP access"
4. Сохраните изменения

### 2. Проблема с аутентификацией

Hostinger может требовать специальный формат аутентификации или дополнительные настройки.

**Проверка:**
- Убедитесь, что используете полный email: `support@cloudcity.center`
- Проверьте, что пароль правильный (попробуйте войти через веб-интерфейс почты)
- Убедитесь, что почтовый ящик активен

### 3. Проблема с SSL/TLS

Hostinger может требовать строгую проверку сертификата или использовать другой метод SSL.

**Решение для диагностики:**
Временно отключите проверку сертификата (только для тестирования):

В файле сервиса добавьте:
```ini
Environment=Email__CheckCertificate=false
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl restart cloudcity
```

⚠️ **ВНИМАНИЕ:** Это только для диагностики! После выяснения проблемы верните обратно.

### 4. Порт или метод подключения

Hostinger может требовать другой порт или метод подключения.

**Альтернативные варианты:**

**Вариант 1: Порт 587 с StartTLS**
```ini
Environment=Email__SmtpPort=587
Environment=Email__UseSsl=true
```

**Вариант 2: Порт 25 (если разрешен)**
```ini
Environment=Email__SmtpPort=25
Environment=Email__UseSsl=false
```

## Диагностика на сервере

### 1. Проверьте логи приложения

```bash
sudo journalctl -u cloudcity -f
```

Отправьте форму и смотрите логи. Должны быть видны:
- Подключение к SMTP
- Аутентификация
- Отправка письма
- Любые ошибки

### 2. Тест подключения к SMTP

```bash
# Проверка доступности порта
telnet smtp.hostinger.com 465

# Или с openssl
openssl s_client -connect smtp.hostinger.com:465 -quiet
```

### 3. Тест аутентификации

Создайте тестовый скрипт:

```bash
# test-smtp.sh
#!/bin/bash
echo "Testing SMTP connection..."
echo "QUIT" | openssl s_client -connect smtp.hostinger.com:465 -starttls smtp
```

### 4. Проверка через почтовый клиент

Попробуйте подключиться к почте через почтовый клиент (например, Thunderbird или Outlook) с теми же данными:
- Host: smtp.hostinger.com
- Port: 465
- SSL/TLS: Включено
- Username: support@cloudcity.center
- Password: ваш пароль

Если почтовый клиент работает, а приложение нет - проблема в коде.
Если почтовый клиент тоже не работает - проблема в настройках Hostinger.

## Что проверить в панели Hostinger

1. **Настройки почтового ящика:**
   - Почтовый ящик `support@cloudcity.center` создан и активен
   - Пароль правильный
   - Почтовый ящик не заблокирован

2. **SMTP настройки:**
   - Разрешены внешние SMTP подключения
   - Порт 465 открыт
   - SSL/TLS включен

3. **Ограничения:**
   - Нет ограничений на количество писем
   - Нет блокировки по IP адресу сервера

4. **Логи Hostinger:**
   - Проверьте логи почтового сервера в панели управления
   - Посмотрите, есть ли попытки подключения
   - Есть ли ошибки аутентификации

## Альтернативные решения

### Вариант 1: Использовать SMTP релей

Если Hostinger не позволяет внешние подключения, можно использовать SMTP релей сервис.

### Вариант 2: Использовать API Hostinger (если доступен)

Некоторые хостинг-провайдеры предоставляют API для отправки писем.

### Вариант 3: Использовать внешний SMTP сервис

- SendGrid
- Mailgun
- Amazon SES
- Или вернуться к Web3Forms

## Улучшенное логирование

Код обновлен с более детальным логированием. Теперь в логах будет видно:
- Каждый шаг подключения
- Результаты аутентификации
- Детальные ошибки на каждом этапе

Проверьте логи после отправки формы:
```bash
sudo journalctl -u cloudcity -n 50 | grep -i "smtp\|email\|auth"
```

## Следующие шаги

1. ✅ Проверьте логи приложения
2. ✅ Проверьте настройки в панели Hostinger
3. ✅ Попробуйте подключиться через почтовый клиент
4. ✅ Если нужно, временно отключите проверку SSL для диагностики
5. ✅ Свяжитесь с поддержкой Hostinger, если проблема не решается

---

**Важно:** После диагностики обязательно верните проверку SSL сертификата обратно!


