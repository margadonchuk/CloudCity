# 🔧 Финальное решение: SMTP порты блокируются

## Диагностика показала

✅ **DNS работает** - `smtp.hostinger.com` резолвится в `172.65.255.143`  
✅ **Ping работает** - пакеты доходят до сервера  
✅ **Traceroute работает** - маршрут проходит  
❌ **TCP подключение зависает** - порты 465/587 блокируются  

## Проблема

Провайдер/VPS **блокирует исходящие TCP подключения на SMTP порты** (465, 587). Это распространенная практика для предотвращения спама.

## Решение 1: Проверка и настройка файрвола (попробуйте сначала)

```bash
# Проверьте UFW
sudo ufw status verbose

# Разрешите исходящие подключения
sudo ufw allow out 587/tcp
sudo ufw allow out 465/tcp
sudo ufw reload

# Проверьте iptables
sudo iptables -L OUTPUT -n -v | grep -E "587|465"

# Если порты блокируются, разрешите
sudo iptables -A OUTPUT -p tcp --dport 587 -j ACCEPT
sudo iptables -A OUTPUT -p tcp --dport 465 -j ACCEPT

# Сохраните правила
sudo iptables-save | sudo tee /etc/iptables/rules.v4
```

## Решение 2: Использовать Web3Forms (РЕКОМЕНДУЕТСЯ)

Так как SMTP порты блокируются провайдером, **лучше использовать Web3Forms**:

### Преимущества:
- ✅ Работает через HTTPS (порт 443, всегда открыт)
- ✅ Не требует SMTP
- ✅ Уже настроено в коде
- ✅ Бесплатно до 250 писем/месяц
- ✅ Простая настройка

### Настройка (5 минут):

1. **Получите API ключ:**
   - Откройте https://web3forms.com
   - Введите email: **support@cloudcity.center**
   - Нажмите "Get Your Access Key"
   - Скопируйте ключ из письма

2. **Обновите код на сервере:**
   ```bash
   cd /home/siteadmin/cloudcity/CloudCityCenter/CloudCity
   git pull
   dotnet build --configuration Release
   sudo systemctl restart cloudcity
   ```

3. **Добавьте API ключ:**
   ```bash
   nano /home/siteadmin/cloudcity/CloudCityCenter/CloudCity/CloudCityCenter/wwwroot/js/web3forms-handler.js
   ```
   
   Найдите строку 9 и замените:
   ```javascript
   const WEB3FORMS_ACCESS_KEY = 'YOUR_WEB3FORMS_ACCESS_KEY';
   ```
   
   На ваш ключ:
   ```javascript
   const WEB3FORMS_ACCESS_KEY = 'ваш-ключ-из-web3forms';
   ```

4. **Убедитесь, что формы используют Web3Forms:**
   - Формы уже настроены (класс `web3form`)
   - JavaScript обработчик подключен в `_Layout.cshtml`

5. **Готово!** Формы будут работать через Web3Forms.

## Решение 3: Связаться с провайдером

Если все же хотите использовать SMTP:

1. Свяжитесь с поддержкой вашего VPS/хостинг провайдера
2. Объясните ситуацию:
   - Нужно отправлять email через SMTP
   - Порты 465 и 587 блокируются
   - Это для легитимной отправки писем с вашего домена
3. Попросите разрешить исходящие подключения на порты 587 и 465
4. Укажите IP адрес вашего сервера

## Рекомендация

**Используйте Web3Forms** - это самое простое и надежное решение:
- Не зависит от блокировок провайдера
- Работает сразу после получения ключа
- Не требует настройки SMTP
- Бесплатно и надежно

---

**После настройки Web3Forms все будет работать!** 🚀


