# 🔒 Безопасность паролей - Важно!

## ✅ Что уже настроено

1. **Файлы конфигурации исключены из Git:**
   - `appsettings.json` - в `.gitignore`
   - `appsettings.Production.json` - в `.gitignore`
   - `appsettings.Development.json` - в `.gitignore`
   - `appsettings.*.json` - все файлы конфигурации исключены

2. **Пароль НЕ хранится в файлах:**
   - Во всех `appsettings*.json` файлах: `"SmtpPassword": ""` (пусто)
   - Пароль должен быть ТОЛЬКО в переменных окружения на сервере

3. **Пример конфигурации:**
   - `appsettings.Example.json` - можно коммитить (без пароля)

## ⚠️ ВАЖНО: Что НЕЛЬЗЯ делать

❌ **НЕ коммитьте файлы с паролями:**
```json
{
  "Email": {
    "SmtpPassword": "реальный_пароль"  // ❌ НИКОГДА ТАК НЕ ДЕЛАЙТЕ!
  }
}
```

❌ **НЕ добавляйте пароль в appsettings.json:**
- Даже локально
- Даже временно
- Даже для тестирования

❌ **НЕ храните пароли в коде:**
- Не в C# файлах
- Не в JavaScript
- Не в комментариях

## ✅ Правильный способ

### На сервере (Linux):

**Вариант 1: Переменные окружения в systemd (рекомендуется)**

```ini
# /etc/systemd/system/cloudcity.service
[Service]
Environment=Email__SmtpPassword=ваш_реальный_пароль
```

**Вариант 2: Локальный файл (не в Git)**

Создайте `appsettings.Local.json` на сервере:
```json
{
  "Email": {
    "SmtpPassword": "ваш_реальный_пароль"
  }
}
```

И добавьте в `.gitignore` (уже добавлено):
```
appsettings.Local.json
```

### На локальной машине (разработка):

Используйте User Secrets (для .NET):
```bash
dotnet user-secrets set "Email:SmtpPassword" "ваш_пароль"
```

Или переменные окружения:
```bash
export Email__SmtpPassword="ваш_пароль"
```

## 🔍 Проверка перед коммитом

Перед каждым коммитом проверяйте:

```bash
# Проверьте, что нет паролей в файлах
git diff | grep -i password

# Проверьте, что appsettings файлы не отслеживаются
git status CloudCityCenter/appsettings*.json

# Если файлы отслеживаются, удалите их из индекса:
git rm --cached CloudCityCenter/appsettings.json
git rm --cached CloudCityCenter/appsettings.Production.json
git rm --cached CloudCityCenter/appsettings.Development.json
```

## 📋 Чек-лист безопасности

- [ ] `appsettings.json` в `.gitignore`
- [ ] `appsettings.Production.json` в `.gitignore`
- [ ] `appsettings.Development.json` в `.gitignore`
- [ ] Пароль пустой во всех appsettings файлах: `"SmtpPassword": ""`
- [ ] Пароль установлен только в переменных окружения на сервере
- [ ] `appsettings.Example.json` создан (можно коммитить)
- [ ] Проверено перед коммитом: `git diff | grep password`

## 🚨 Если пароль случайно попал в Git

Если вы случайно закоммитили пароль:

1. **Немедленно смените пароль** в панели Hostinger!

2. **Удалите из истории Git:**
   ```bash
   # Удалите файл из индекса
   git rm --cached CloudCityCenter/appsettings.json
   
   # Если уже закоммитили, удалите из истории
   git filter-branch --force --index-filter \
     "git rm --cached --ignore-unmatch CloudCityCenter/appsettings.json" \
     --prune-empty --tag-name-filter cat -- --all
   ```

3. **Обновите .gitignore** (уже сделано)

4. **Сделайте force push** (если уже запушили):
   ```bash
   git push origin --force --all
   ```
   ⚠️ **ВНИМАНИЕ:** Это перезапишет историю. Убедитесь, что все в команде знают об этом.

## 📝 Резюме

✅ **Пароль должен быть:**
- В переменных окружения на сервере
- В User Secrets для локальной разработки
- В `appsettings.Local.json` (не в Git)

❌ **Пароль НЕ должен быть:**
- В `appsettings.json` (в Git)
- В `appsettings.Production.json` (в Git)
- В коде
- В коммитах

---

**Помните:** Безопасность важнее удобства! Всегда используйте переменные окружения для паролей.

