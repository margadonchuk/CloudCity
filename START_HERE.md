# 🚀 ЗАПУСК САЙТА НА ДОМЕНЕ - ПРОСТАЯ ИНСТРУКЦИЯ

## ⚠️ САЙТ НЕ ОТКРЫВАЕТСЯ?

**Сначала запустите диагностику:**

```bash
cd /home/siteadmin/cloudcity
git pull
chmod +x diagnose.sh
./diagnose.sh
```

Этот скрипт покажет что именно не работает!

---

## Если сервис не запущен (inactive/dead):

```bash
cd /home/siteadmin/cloudcity
git pull
chmod +x fix-service.sh
./fix-service.sh
```

## Если сервиса еще нет - первая настройка:

```bash
cd /home/siteadmin/cloudcity
git pull
chmod +x setup-production.sh
./setup-production.sh
```

**ВСЕ!** Скрипт сам:
- ✅ Соберет приложение
- ✅ Настроит systemd сервис
- ✅ Запустит приложение
- ✅ Проверит что все работает

После выполнения скрипта ваш сайт будет доступен:
**https://cloudcitylife.com**

---

## Почему localhost:5000?

Это правильная архитектура:

```
Ваш браузер
    ↓ HTTPS
Nginx (443 порт) → обрабатывает SSL сертификат для cloudcitylife.com
    ↓ HTTP (внутри сервера)
Приложение (localhost:5000) → ваше ASP.NET Core приложение
```

**Nginx** получает HTTPS запросы на домене и проксирует их на ваше приложение.
**Приложение** слушает только внутри сервера (localhost), это безопасно.

---

## Если что-то не работает:

### 0. Запустите диагностику:
```bash
./diagnose.sh
```

### 1. Проверьте что приложение запущено:
```bash
sudo systemctl status cloudcity
```

### 2. Посмотрите логи:
```bash
sudo journalctl -u cloudcity -f
```

### 3. Проверьте nginx:
```bash
sudo nginx -t
sudo systemctl status nginx
```

### 4. Проверьте порты:
```bash
curl http://localhost:5000          # должно работать
curl https://cloudcitylife.com      # должно работать
```

### 5. Проверьте nginx конфигурацию:
Смотрите файл `CHECK_NGINX.md` в репозитории

---

## Управление:

```bash
# Перезапустить приложение
sudo systemctl restart cloudcity

# Остановить
sudo systemctl stop cloudcity

# Запустить
sudo systemctl start cloudcity

# Логи в реальном времени
sudo journalctl -u cloudcity -f
```

