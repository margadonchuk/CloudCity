# 🔑 Установка Web3Forms API ключа

## Проблема

Форма показывает ошибку, потому что API ключ Web3Forms не установлен.

## Решение

### Шаг 1: Получите API ключ

1. Откройте https://web3forms.com
2. Введите email: **support@cloudcity.center**
3. Нажмите "Get Your Access Key"
4. Проверьте почту и скопируйте ключ из письма

### Шаг 2: Обновите файл на сервере

**На сервере выполните:**

```bash
# Откройте файл для редактирования
nano /home/siteadmin/cloudcity/CloudCityCenter/CloudCity/CloudCityCenter/wwwroot/js/web3forms-handler.js
```

**Найдите строку 9:**
```javascript
const WEB3FORMS_ACCESS_KEY = 'YOUR_WEB3FORMS_ACCESS_KEY';
```

**Замените на:**
```javascript
const WEB3FORMS_ACCESS_KEY = 'ваш-реальный-ключ-из-письма';
```

**Сохраните:** Ctrl+O, Enter, Ctrl+X

### Шаг 3: Проверьте

1. Откройте сайт в браузере
2. Нажмите F12 (DevTools)
3. Перейдите на вкладку "Console"
4. Обновите страницу (Ctrl+Shift+R для жесткой перезагрузки)
5. Проверьте, что нет ошибки "API ключ не установлен"

### Шаг 4: Протестируйте форму

1. Заполните форму на странице Contact или About
2. Отправьте форму
3. Проверьте консоль браузера - должно быть:
   ```
   ✅ Web3Forms: Письмо успешно отправлено
   ```

## Важно

- **Не нужно перезапускать сервис** - JavaScript загружается напрямую в браузере
- **Очистите кеш браузера** после изменения файла (Ctrl+Shift+R)
- **Ключ должен быть в одинарных кавычках** и без пробелов

## Альтернатива: Установка через переменные окружения

Если хотите управлять ключом через переменные окружения (но это сложнее для JavaScript):

1. Можно добавить ключ в `appsettings.json` и передать через серверный рендеринг
2. Но проще просто обновить файл `web3forms-handler.js` напрямую

---

**После установки ключа формы должны работать!**

