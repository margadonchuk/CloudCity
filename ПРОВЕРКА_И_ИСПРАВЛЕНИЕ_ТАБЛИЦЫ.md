# 🔍 Проверка и исправление таблицы ContactMessages

## Шаг 1: Проверьте структуру таблицы

Выполните в SQL Server Management Studio:

```sql
USE CloudCityDB;
GO

-- Проверка структуры таблицы
SELECT 
    COLUMN_NAME,
    DATA_TYPE,
    CHARACTER_MAXIMUM_LENGTH,
    IS_NULLABLE,
    COLUMN_DEFAULT
FROM INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_NAME = 'ContactMessages'
ORDER BY ORDINAL_POSITION;
GO
```

## Шаг 2: Проверьте правильность структуры

Таблица должна иметь такую структуру:

| Column | Type | Length | Nullable | Default |
|--------|------|--------|----------|---------|
| Id | INT | - | NO | IDENTITY(1,1) |
| Name | NVARCHAR | 200 | NO | - |
| Email | NVARCHAR | 200 | NO | - |
| Phone | NVARCHAR | 50 | YES | - |
| Subject | NVARCHAR | 200 | YES | - |
| ServiceType | NVARCHAR | 100 | YES | - |
| Message | NVARCHAR(MAX) | - | NO | - |
| SourcePage | NVARCHAR | 50 | NO | - |
| CreatedAt | DATETIME2 | - | NO | GETUTCDATE() |
| IsRead | BIT | - | NO | 0 |
| ReadAt | DATETIME2 | - | YES | - |

## Шаг 3: Если структура неправильная

### Вариант A: Пересоздать таблицу

```sql
USE CloudCityDB;
GO

-- Удалить таблицу (ВНИМАНИЕ: удалит все данные!)
DROP TABLE IF EXISTS [dbo].[ContactMessages];
GO

-- Создать таблицу заново
CREATE TABLE [dbo].[ContactMessages] (
    [Id] INT NOT NULL IDENTITY(1,1),
    [Name] NVARCHAR(200) NOT NULL,
    [Email] NVARCHAR(200) NOT NULL,
    [Phone] NVARCHAR(50) NULL,
    [Subject] NVARCHAR(200) NULL,
    [ServiceType] NVARCHAR(100) NULL,
    [Message] NVARCHAR(MAX) NOT NULL,
    [SourcePage] NVARCHAR(50) NOT NULL,
    [CreatedAt] DATETIME2 NOT NULL DEFAULT GETUTCDATE(),
    [IsRead] BIT NOT NULL DEFAULT 0,
    [ReadAt] DATETIME2 NULL,
    CONSTRAINT [PK_ContactMessages] PRIMARY KEY CLUSTERED ([Id] ASC)
);
GO

PRINT 'Таблица ContactMessages пересоздана';
GO
```

### Вариант B: Исправить существующую таблицу

Если нужно сохранить данные, исправьте только проблемные колонки:

```sql
USE CloudCityDB;
GO

-- Пример: если Id не INT, исправить нельзя без пересоздания
-- Но можно исправить другие колонки

-- Если Name не NVARCHAR(200)
ALTER TABLE [dbo].[ContactMessages]
ALTER COLUMN [Name] NVARCHAR(200) NOT NULL;
GO

-- Если Email не NVARCHAR(200)
ALTER TABLE [dbo].[ContactMessages]
ALTER COLUMN [Email] NVARCHAR(200) NOT NULL;
GO

-- И так далее для всех колонок
```

## Шаг 4: Пометить миграцию как примененную

После того как таблица создана правильно, нужно пометить миграцию `AddContactMessages` как примененную:

```sql
USE CloudCityDB;
GO

-- Проверьте, есть ли запись о миграции
SELECT * FROM [dbo].[__EFMigrationsHistory] 
WHERE [MigrationId] LIKE '%ContactMessages%';
GO

-- Если миграции нет в истории, добавьте её вручную
-- Найдите имя миграции (например, 20250106191202_AddContactMessages)
-- И добавьте запись:

INSERT INTO [dbo].[__EFMigrationsHistory] ([MigrationId], [ProductVersion])
VALUES ('20250106191202_AddContactMessages', '8.0.6');
GO
```

**Важно:** Замените `20250106191202_AddContactMessages` на реальное имя вашей миграции!

## Шаг 5: Проверка работы

1. Перезапустите сервис:
   ```bash
   sudo systemctl restart cloudcity
   ```

2. Откройте `/Admin/Messages` в браузере

3. Отправьте тестовую форму на странице Contact или About

4. Проверьте, что письмо появилось в `/Admin/Messages`

## Если проблема с InitialCreate миграцией

Если миграция `InitialCreate` все еще пытается применяться и падает на `AspNetRoles`, можно:

1. Пометить её как примененную (если таблицы Identity уже существуют):
   ```sql
   INSERT INTO [dbo].[__EFMigrationsHistory] ([MigrationId], [ProductVersion])
   VALUES ('20250911222054_InitialCreate', '8.0.6');
   GO
   ```

2. Или удалить проблемную миграцию и создать новую для SQL Server

---

**После исправления таблицы все должно работать!**

