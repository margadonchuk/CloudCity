# 📸 Как изменить картинки товаров

## 📁 Где хранятся картинки

**Локально (на вашем компьютере):**
```
CloudCityCenter/wwwroot/images/
```

**На сервере:**
```
/home/siteadmin/cloudcity/CloudCityCenter/wwwroot/images/
```

## 🖼️ Текущие картинки товаров

### Dedicated Servers:
- DS1-E3 → `/images/dell.png`
- DS2-E5 → `/images/hp.png`
- DS3-Ryzen → `/images/ovh.png`
- DS4-EPYC → `/images/hetzner.png`
- DS5-Storage → `/images/proxmox.png`
- DS6-HighMem → `/images/ms.png`

### VPS:
- VPS1-1-20 → `/images/vps1.png`
- VPS2-2-20 → `/images/vps2.png`
- VPS3-4-40 → `/images/vps3.png`
- VPS4-4-80 → `/images/vps4.png`
- VPS5-4-80 → `/images/vps5.png`
- VPS6-6-100 → `/images/vps6.png`

### VPN:
- VPN for a network → `/images/vpn-network.png`
- VPN for a device → `/images/vpn-device.png`

## ✅ Способ 1: Замена файлов (рекомендуется)

### Шаг 1: Подготовьте новые картинки

1. Создайте новые изображения с теми же именами:
   - `dell.png` для DS1-E3
   - `hp.png` для DS2-E5
   - `vps1.png` для VPS1-1-20
   - и т.д.

2. **Рекомендуемый размер:** 300x200 пикселей (или пропорционально)
3. **Формат:** PNG или JPG

### Шаг 2: Замените файлы локально

```bash
# На вашем компьютере
cd CloudCityCenter/wwwroot/images

# Замените нужные файлы
# Например, замените dell.png на новую картинку
```

### Шаг 3: Загрузите на сервер

```bash
# На сервере
cd /home/siteadmin/cloudcity/CloudCityCenter/wwwroot/images

# Загрузите новые файлы (через scp, sftp или git)
# Например:
scp dell.png siteadmin@10.151.10.10:/home/siteadmin/cloudcity/CloudCityCenter/wwwroot/images/
```

### Шаг 4: Перезапустите приложение (если нужно)

Обычно статические файлы (изображения) обновляются автоматически, но если не видно изменений:

```bash
sudo systemctl restart cloudcity
```

## ✅ Способ 2: Изменение через базу данных

Если хотите использовать другие файлы или внешние URL:

### Шаг 1: Загрузите новое изображение

Загрузите файл в `wwwroot/images/` с новым именем, например `ds1-new.png`

### Шаг 2: Обновите в базе данных

```sql
USE CloudCityDB;

-- Измените картинку для конкретного товара
UPDATE Products 
SET ImageUrl = '/images/ds1-new.png' 
WHERE Name = 'DS1-E3';

-- Или используйте внешний URL
UPDATE Products 
SET ImageUrl = 'https://example.com/image.png' 
WHERE Name = 'DS1-E3';

-- Проверьте результат
SELECT Name, ImageUrl FROM Products WHERE Name = 'DS1-E3';
```

### Шаг 3: Проверьте на сайте

Обновите страницу https://cloudcitylife.com/Servers - картинка должна измениться.

## 📋 Массовое изменение картинок

Если нужно изменить картинки для всех товаров определенного типа:

```sql
USE CloudCityDB;

-- Изменить все Dedicated Servers
UPDATE Products 
SET ImageUrl = '/images/new-server.png' 
WHERE Type = 0;  -- 0 = DedicatedServer

-- Изменить все VPS
UPDATE Products 
SET ImageUrl = '/images/new-vps.png' 
WHERE Type = 3;  -- 3 = VPS

-- Изменить все VPN
UPDATE Products 
SET ImageUrl = '/images/new-vpn.png' 
WHERE Type = 4;  -- 4 = VPN
```

## 🔍 Проверка текущих картинок в базе

```sql
USE CloudCityDB;

-- Посмотреть все картинки товаров
SELECT 
    Name,
    Type,
    ImageUrl,
    CASE Type
        WHEN 0 THEN 'Dedicated Server'
        WHEN 1 THEN 'Hosting'
        WHEN 2 THEN 'Website'
        WHEN 3 THEN 'VPS'
        WHEN 4 THEN 'VPN'
    END as TypeName
FROM Products
ORDER BY Type, Name;
```

## 📝 Список всех изображений товаров

| Товар | Текущая картинка | Путь на сервере |
|-------|------------------|-----------------|
| DS1-E3 | dell.png | `/wwwroot/images/dell.png` |
| DS2-E5 | hp.png | `/wwwroot/images/hp.png` |
| DS3-Ryzen | ovh.png | `/wwwroot/images/ovh.png` |
| DS4-EPYC | hetzner.png | `/wwwroot/images/hetzner.png` |
| DS5-Storage | proxmox.png | `/wwwroot/images/proxmox.png` |
| DS6-HighMem | ms.png | `/wwwroot/images/ms.png` |
| VPS1-1-20 | vps1.png | `/wwwroot/images/vps1.png` |
| VPS2-2-20 | vps2.png | `/wwwroot/images/vps2.png` |
| VPS3-4-40 | vps3.png | `/wwwroot/images/vps3.png` |
| VPS4-4-80 | vps4.png | `/wwwroot/images/vps4.png` |
| VPS5-4-80 | vps5.png | `/wwwroot/images/vps5.png` |
| VPS6-6-100 | vps6.png | `/wwwroot/images/vps6.png` |
| VPN Network | vpn-network.png | `/wwwroot/images/vpn-network.png` |
| VPN Device | vpn-device.png | `/wwwroot/images/vpn-device.png` |

## ⚠️ Важно

1. **Имена файлов:** Если заменяете файлы, используйте те же имена, чтобы не менять базу данных
2. **Размер файлов:** Оптимизируйте изображения для веба (обычно < 200KB)
3. **Формат:** PNG для логотипов, JPG для фотографий
4. **Пути:** Все пути начинаются с `/images/` (относительно wwwroot)

## 🚀 Быстрая замена одной картинки

```bash
# 1. На сервере перейдите в папку с картинками
cd /home/siteadmin/cloudcity/CloudCityCenter/wwwroot/images

# 2. Загрузите новую картинку (через scp, sftp или замените файл)
# Например, замените dell.png

# 3. Проверьте права доступа
chmod 644 dell.png

# 4. Обновите страницу сайта - изменения должны быть видны сразу!
```

## 🔄 Если картинка не обновляется

1. **Очистите кеш браузера:** Ctrl+F5
2. **Проверьте путь:** Убедитесь что файл существует
3. **Проверьте права:** `chmod 644 filename.png`
4. **Перезапустите приложение:** `sudo systemctl restart cloudcity`




