# 🔧 Решение проблемы подключения к SMTP

## Проблема

В логах видно:
- ✅ Форма отправляется успешно
- ✅ Начинается подключение к SMTP: "Attempting to connect to smtp.hostinger.com:465..."
- ❌ Подключение зависает - нет дальнейших логов
- ❌ Письмо не отправляется

## Что сделано

1. ✅ Добавлены таймауты для всех операций SMTP:
   - Подключение: 30 секунд
   - Аутентификация: 15 секунд
   - Отправка: 30 секунд

2. ✅ Улучшена обработка ошибок с детальным логированием

3. ✅ Добавлена обработка таймаутов

## Возможные причины зависания

### 1. Hostinger блокирует подключение

**Симптомы:** Подключение зависает без ошибки

**Решение:**
1. Проверьте в панели Hostinger:
   - Разрешены ли внешние SMTP подключения?
   - Нет ли блокировки по IP адресу сервера?
   - Активен ли почтовый ящик?

2. Попробуйте другой порт:
   ```ini
   Environment=Email__SmtpPort=587
   Environment=Email__UseSsl=true
   ```

### 2. Проблема с SSL/TLS

**Симптомы:** Подключение не устанавливается на порту 465

**Решение для диагностики:**
Временно отключите проверку сертификата:
```ini
Environment=Email__CheckCertificate=false
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl restart cloudcity
```

⚠️ **ВНИМАНИЕ:** Только для диагностики! После выяснения проблемы верните обратно.

### 3. Файрвол блокирует порт 465

**Проверка:**
```bash
# Проверьте доступность порта
telnet smtp.hostinger.com 465

# Или
nc -zv smtp.hostinger.com 465
```

### 4. Проблема с DNS

**Проверка:**
```bash
nslookup smtp.hostinger.com
ping smtp.hostinger.com
```

## Что делать сейчас

### 1. Обновите код на сервере:

```bash
cd /home/siteadmin/cloudcity/CloudCityCenter/CloudCity
git pull
dotnet build --configuration Release
sudo systemctl restart cloudcity
```

### 2. Отправьте форму и проверьте логи:

```bash
sudo journalctl -u cloudcity -f
```

Теперь должны быть видны:
- Либо успешное подключение
- Либо ошибка таймаута через 30 секунд
- Либо другая ошибка с детальным описанием

### 3. Если видите таймаут:

```
Connection to smtp.hostinger.com:465 timed out after 30 seconds
```

Это означает, что Hostinger не отвечает на подключение. Проверьте:
1. Настройки в панели Hostinger
2. Попробуйте порт 587 вместо 465
3. Проверьте файрвол

### 4. Альтернативные настройки для тестирования:

**Вариант 1: Порт 587 с StartTLS**
```ini
Environment=Email__SmtpPort=587
Environment=Email__UseSsl=true
```

**Вариант 2: Без SSL (только для теста)**
```ini
Environment=Email__SmtpPort=25
Environment=Email__UseSsl=false
```

**Вариант 3: Отключить проверку сертификата**
```ini
Environment=Email__CheckCertificate=false
```

## Проверка через командную строку

Создайте тестовый скрипт:

```bash
# test-smtp-direct.sh
#!/bin/bash
echo "Testing direct SMTP connection..."
timeout 10 openssl s_client -connect smtp.hostinger.com:465 -quiet
```

Если это тоже зависает - проблема в сетевых настройках или Hostinger блокирует.

## Контакты Hostinger

Если ничего не помогает:
1. Соберите логи: `sudo journalctl -u cloudcity -n 100 > email-logs.txt`
2. Свяжитесь с поддержкой Hostinger
3. Спросите:
   - Разрешены ли внешние SMTP подключения?
   - Какой порт использовать для SMTP?
   - Нужны ли специальные настройки?

## Альтернативное решение

Если Hostinger не позволяет внешние SMTP подключения, рассмотрите:
- Web3Forms (уже настроено в коде)
- SendGrid
- Mailgun
- Amazon SES

---

**После обновления кода проверьте логи - теперь будет видно, где именно происходит проблема!**


