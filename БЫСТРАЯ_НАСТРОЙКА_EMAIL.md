# 🚀 Быстрая настройка Email (Hostinger SMTP)

## ✅ Что уже готово

- ✅ EmailService настроен для Hostinger SMTP (порт 465, SSL)
- ✅ Формы на страницах About и Contact работают
- ✅ Пароль НЕ хранится в Git

## 📝 Что нужно сделать на сервере (2 минуты)

### Вариант 1: Автоматическая настройка (рекомендуется)

1. Загрузите скрипт на сервер:
   ```bash
   # Скопируйте setup-email-env.sh на сервер
   ```

2. Запустите скрипт:
   ```bash
   sudo chmod +x setup-email-env.sh
   sudo ./setup-email-env.sh
   ```

3. Введите пароль от почты support@cloudcity.center

4. Готово! ✅

### Вариант 2: Ручная настройка

1. Откройте файл сервиса:
   ```bash
   sudo nano /etc/systemd/system/cloudcity.service
   ```

2. Добавьте перед `[Install]`:
   ```ini
   # Email настройки (Hostinger SMTP)
   Environment=Email__SmtpHost=smtp.hostinger.com
   Environment=Email__SmtpPort=465
   Environment=Email__UseSsl=true
   Environment=Email__SmtpUsername=support@cloudcity.center
   Environment=Email__SmtpPassword=ВАШ_ПАРОЛЬ_ОТ_ПОЧТЫ
   Environment=Email__RecipientEmail=support@cloudcity.center
   ```

3. Перезагрузите сервис:
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl restart cloudcity
   ```

4. Проверьте логи:
   ```bash
   sudo journalctl -u cloudcity -f
   ```

## ✅ Проверка

1. Откройте сайт
2. Заполните форму на странице **About** или **Contact**
3. Отправьте форму
4. Проверьте почтовый ящик **support@cloudcity.center**

## 🔒 Безопасность

✅ Пароль хранится только в переменных окружения на сервере  
✅ Пароль НЕ в Git  
✅ Пароль НЕ в файлах конфигурации в репозитории  

---

**Подробная инструкция:** см. `НАСТРОЙКА_EMAIL_HOSTINGER.md`


