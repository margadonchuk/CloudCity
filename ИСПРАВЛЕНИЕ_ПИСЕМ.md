# 🔧 Исправление проблемы с письмами

## Проблема

Страница `/Admin/Messages` не работает, потому что:
1. Миграция не была применена (таблица `ContactMessages` не существует)
2. При попытке открыть страницу возникает ошибка

## Решение

### Шаг 1: Применить миграцию на сервере

**Важно:** Нужно установить строку подключения перед применением миграции!

```bash
cd /home/siteadmin/cloudcity/CloudCityCenter

# Установите строку подключения (замените на вашу)
export ConnectionStrings__DefaultConnection="Server=10.151.10.8;Database=CloudCityDB;User Id=ваш_логин;Password=ваш_пароль;TrustServerCertificate=True"

# Примените миграцию
dotnet ef database update --project CloudCityCenter

# Перезапустите сервис
sudo systemctl restart cloudcity
```

### Шаг 2: Проверка

1. Откройте `/Admin/Messages` в браузере
2. Если видите ошибку - проверьте логи:
   ```bash
   sudo journalctl -u cloudcity -n 50 --no-pager
   ```

### Шаг 3: Если миграция не существует

Если миграция `AddContactMessages` не была создана, создайте её:

```bash
cd /home/siteadmin/cloudcity/CloudCityCenter

# Установите строку подключения
export ConnectionStrings__DefaultConnection="Server=10.151.10.8;Database=CloudCityDB;User Id=ваш_логин;Password=ваш_пароль;TrustServerCertificate=True"

# Создайте миграцию
dotnet ef migrations add AddContactMessages --project CloudCityCenter

# Примените миграцию
dotnet ef database update --project CloudCityCenter

# Перезапустите сервис
sudo systemctl restart cloudcity
```

### Шаг 4: Проверка таблицы в БД

Если хотите проверить, что таблица создана:

```bash
# Подключитесь к SQL Server (если есть доступ)
sqlcmd -S 10.151.10.8 -U ваш_логин -P ваш_пароль -d CloudCityDB -Q "SELECT COUNT(*) FROM ContactMessages"
```

Или через приложение - просто откройте `/Admin/Messages` и проверьте, что страница загружается.

## Альтернатива: Автоматическое применение миграций

Миграции применяются автоматически при запуске приложения (см. `Program.cs`), если:
- Установлена строка подключения через переменные окружения в systemd
- Приложение запускается с флагом `--migrate-data` или `--seed`

Проверьте файл сервиса:
```bash
sudo systemctl show cloudcity | grep ConnectionStrings
```

Если строка подключения установлена, просто перезапустите сервис - миграции применятся автоматически.

## Если все еще не работает

1. Проверьте логи:
   ```bash
   sudo journalctl -u cloudcity -f
   ```

2. Откройте страницу `/Admin/Messages` и смотрите, какая ошибка появляется в логах

3. Убедитесь, что вы вошли как администратор (роль "Admin")

---

**После применения миграции письма должны работать!**


