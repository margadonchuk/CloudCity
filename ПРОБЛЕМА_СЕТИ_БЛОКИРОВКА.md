# 🚨 Проблема: Сеть блокирует SMTP порты

## Симптомы

```
Trying 172.65.255.143...
```

Подключение зависает на этапе "Trying..." - это означает, что:
- DNS работает (IP адрес получен: 172.65.255.143)
- Но TCP соединение не устанавливается
- Скорее всего, файрвол или провайдер блокирует порты 465 и 587

## Диагностика

### 1. Проверьте файрвол на сервере:

```bash
# Проверьте статус
sudo ufw status verbose

# Если файрвол активен, разрешите порты
sudo ufw allow 587/tcp
sudo ufw allow 465/tcp

# Или временно отключите для теста
sudo ufw disable
# (после теста включите обратно: sudo ufw enable)
```

### 2. Проверьте iptables:

```bash
# Посмотрите правила
sudo iptables -L -n -v | grep 587
sudo iptables -L -n -v | grep 465

# Если есть блокировки, разрешите
sudo iptables -A OUTPUT -p tcp --dport 587 -j ACCEPT
sudo iptables -A OUTPUT -p tcp --dport 465 -j ACCEPT
```

### 3. Проверьте, не блокирует ли провайдер:

Многие провайдеры/VPS блокируют исходящие подключения на порты 25, 465, 587 для предотвращения спама.

**Проверка:**
- Свяжитесь с поддержкой вашего VPS/хостинг провайдера
- Спросите, не блокируют ли они исходящие SMTP подключения
- Попросите разрешить порты 587 и 465 для исходящих подключений

### 4. Альтернативная проверка:

```bash
# Используйте nc (netcat) вместо telnet
timeout 10 nc -zv smtp.hostinger.com 587
timeout 10 nc -zv smtp.hostinger.com 465

# Или используйте curl для теста
timeout 10 curl -v telnet://smtp.hostinger.com:587
```

## Решения

### Решение 1: Разрешить порты в файрволе

```bash
sudo ufw allow out 587/tcp
sudo ufw allow out 465/tcp
sudo ufw reload
```

### Решение 2: Использовать другой SMTP сервис

Если провайдер блокирует SMTP порты, используйте внешний SMTP сервис:

**Вариант A: SendGrid**
- Бесплатно до 100 писем/день
- Не требует открытия портов
- API ключ вместо SMTP

**Вариант B: Mailgun**
- Бесплатно до 5000 писем/месяц
- API или SMTP

**Вариант C: Web3Forms** (уже настроено)
- Бесплатно до 250 писем/месяц
- Работает через API, не требует SMTP

### Решение 3: Использовать SMTP релей

Настройте SMTP релей на другом сервере, который не блокирует порты.

### Решение 4: Связаться с провайдером

Если вы используете VPS или хостинг:
1. Свяжитесь с поддержкой
2. Объясните, что нужно отправлять email через SMTP
3. Попросите разрешить исходящие подключения на порты 587 и 465
4. Укажите, что это для легитимной отправки писем с вашего домена

## Рекомендация

Если файрвол не помогает и провайдер блокирует порты, **лучше всего использовать Web3Forms**:

1. ✅ Уже настроено в коде
2. ✅ Не требует открытия портов
3. ✅ Работает через HTTPS (порт 443)
4. ✅ Бесплатно до 250 писем/месяц
5. ✅ Простая настройка (только API ключ)

## Быстрая проверка файрвола

```bash
# Проверьте все правила
sudo ufw status numbered

# Временно отключите файрвол
sudo ufw disable

# Попробуйте подключение снова
timeout 10 telnet smtp.hostinger.com 587

# Если заработало - проблема в файрволе
# Включите обратно и разрешите порты:
sudo ufw enable
sudo ufw allow out 587/tcp
sudo ufw allow out 465/tcp
```

---

**Скорее всего проблема в файрволе или блокировке провайдера. Проверьте файрвол первым делом!**

