# 🚨 Hostinger блокирует SMTP подключения

## Проблема

Оба порта таймаутят:
- ❌ Порт 465: `Connection timed out`
- ❌ Порт 587: `Connection timed out after 30 seconds`

Это означает, что **Hostinger блокирует внешние SMTP подключения** с вашего сервера.

## Решение: Использовать Web3Forms

Так как Hostinger не позволяет внешние SMTP подключения, лучше использовать **Web3Forms** - это работает без SMTP и проще в настройке.

### Шаг 1: Получите API ключ

1. Откройте https://web3forms.com
2. Введите email: **support@cloudcity.center**
3. Нажмите "Get Your Access Key"
4. Скопируйте ключ из письма

### Шаг 2: Обновите код

На сервере откройте:
```bash
nano /home/siteadmin/cloudcity/CloudCityCenter/CloudCity/CloudCityCenter/wwwroot/js/web3forms-handler.js
```

Найдите строку 8:
```javascript
const WEB3FORMS_ACCESS_KEY = 'YOUR_WEB3FORMS_ACCESS_KEY';
```

Замените на ваш ключ:
```javascript
const WEB3FORMS_ACCESS_KEY = 'ваш-ключ-из-web3forms';
```

### Шаг 3: Обновите формы

Убедитесь, что формы имеют класс `web3form`:

**Contact/Index.cshtml:**
```html
<form class="web3form contact-form" data-source-page="Contact">
```

**About/Index.cshtml:**
```html
<form class="web3form" data-source-page="About">
```

### Шаг 4: Перезапустите

```bash
cd /home/siteadmin/cloudcity/CloudCityCenter/CloudCity
git pull
dotnet build --configuration Release
sudo systemctl restart cloudcity
```

## Альтернатива: Настроить SMTP в Hostinger

Если все же хотите использовать SMTP:

1. **Свяжитесь с поддержкой Hostinger:**
   - Спросите, как разрешить внешние SMTP подключения
   - Уточните, какой порт использовать
   - Нужны ли специальные настройки

2. **Проверьте в панели Hostinger:**
   - Настройки почты для домена
   - Разрешения для SMTP
   - Блокировки по IP

## Преимущества Web3Forms

✅ Работает без SMTP  
✅ Не требует настройки сервера  
✅ Бесплатно до 250 писем/месяц  
✅ Защита от спама встроена  
✅ Работает сразу после получения ключа  

## Что уже настроено

- ✅ JavaScript обработчик (`web3forms-handler.js`)
- ✅ Формы готовы к использованию
- ✅ Нужно только добавить API ключ

---

**Рекомендация:** Используйте Web3Forms - это самый простой и надежный вариант, если Hostinger блокирует SMTP.


