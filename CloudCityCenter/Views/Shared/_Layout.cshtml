@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer Localizer
@using Microsoft.AspNetCore.Http
@using System.Globalization
<!DOCTYPE html>
<html lang="@CultureInfo.CurrentCulture.Name">
<head>
    
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - CloudCityCenter</title>
    <link rel="icon" type="image/png" href="~/images/logo.png">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    @RenderSection("Styles", required: false)


</head>

<body>
    @{
        var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
        var cartCount = 0;
        try
        {
            var cartJson = Context.Session?.GetString("Cart");
            if (!string.IsNullOrEmpty(cartJson))
            {
                var cartItems = System.Text.Json.JsonSerializer.Deserialize<List<CloudCityCenter.Models.OrderItem>>(cartJson);
                cartCount = cartItems?.Count ?? 0;
            }
        }
        catch
        {
            // Игнорируем ошибки сессии
            cartCount = 0;
        }
    }
    <header>
        <nav class="navbar navbar-expand-lg navbar-modern fixed-top">
            <div class="container-fluid" style="position: relative;">
                <!-- Логотип - слева -->
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">
                    <img src="~/images/logo.png" alt="CloudCityCenter Logo" class="navbar-logo">
                </a>


                <!-- Мобильная версия: Соц сети по центру -->
                <div class="d-flex align-items-center justify-content-center d-lg-none mobile-nav-center">
                    <a class="nav-link instagram-link-mobile" href="https://instagram.com/cloudcity.center" target="_blank" rel="noopener noreferrer">
                        <div class="instagram-wrapper-mobile">
                            <i class="bi bi-instagram instagram-icon-mobile"></i>
                        </div>
                    </a>
                    <a class="nav-link telegram-bot-link-mobile" href="https://t.me/CloudCityCenter_Bot" target="_blank">
                        <div class="telegram-bot-wrapper-mobile">
                            <i class="bi bi-telegram telegram-icon-mobile"></i>
                        </div>
                    </a>
                    <a class="nav-link whatsapp-link-mobile" href="https://wa.me/33773166380" target="_blank" rel="noopener noreferrer">
                        <div class="whatsapp-wrapper-mobile">
                            <i class="bi bi-whatsapp whatsapp-icon-mobile"></i>
                        </div>
                    </a>
                </div>

                <!-- Мобильная версия: Корзина и Меню - справа -->
                <div class="d-flex align-items-center gap-2 d-lg-none mobile-nav-right" style="display: flex !important; visibility: visible !important; opacity: 1 !important; position: relative !important; z-index: 9998 !important; flex: 0 0 auto !important; margin-left: auto !important; width: auto !important; height: auto !important;">
                    <a class="nav-link cart-link-mobile position-relative" asp-controller="Cart" asp-action="Index">
                        <i class="bi bi-cart3"></i>
                        <span id="cart-count-mobile-header" class="cart-badge-mobile" @(cartCount == 0 ? "style=\"display:none;\"" : "")>@cartCount</span>
                    </a>
                    <button id="mobileMenuBtn" type="button" class="navbar-toggler mobile-menu-btn d-lg-none" data-bs-toggle="offcanvas" data-bs-target="#mobileMenu" aria-controls="mobileMenu" aria-label="Открыть меню">
                        <span class="navbar-toggler-icon" aria-hidden="true"></span>
                        <i class="bi bi-list" aria-hidden="true"></i>
                    </button>
                </div>


                <div class="collapse navbar-collapse show d-lg-block" id="navbarNav">
                    <div class="d-flex align-items-center w-100 position-relative">
                        <!-- Left Navigation -->
                        <ul class="navbar-nav navbar-nav-left">
                            <li class="nav-item">
                                <a class="nav-link @(currentController == "Servers" ? "active" : "")" asp-area="" asp-controller="Servers" asp-action="Index">
                                    <i class="bi bi-server me-1"></i>@Localizer["Servers"]
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link @(currentController == "Hosting" ? "active" : "")" asp-area="" asp-controller="Hosting" asp-action="Index">
                                    <i class="bi bi-globe me-1"></i>@Localizer["Hosting"]
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link @(string.Equals(currentController, "VPN", StringComparison.OrdinalIgnoreCase) ? "active" : "")" asp-area="" asp-controller="Vpn" asp-action="Index">
                                    <i class="bi bi-shield-lock me-1"></i>@Localizer["VPN"]
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link @(string.Equals(currentController, "VPS", StringComparison.OrdinalIgnoreCase) ? "active" : "")" asp-area="" asp-controller="Vps" asp-action="Index">
                                    <i class="bi bi-hdd-stack me-1"></i>@Localizer["VPS"]
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link @(string.Equals(currentController, "VDI", StringComparison.OrdinalIgnoreCase) ? "active" : "")" asp-area="" asp-controller="VDI" asp-action="Index">
                                    <i class="bi bi-display me-1"></i>@Localizer["VDI"]
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link @(currentController == "Services" ? "active" : "")" asp-area="" asp-controller="Services" asp-action="Index">
                                    <i class="bi bi-gear me-1"></i>@Localizer["Services"]
                                </a>
                            </li>
                        </ul>

                        <!-- Center - Social Media Links -->
                        <div class="navbar-center-social d-none d-lg-flex align-items-center gap-2">
                            <a class="nav-link instagram-link" href="https://instagram.com/cloudcity.center" target="_blank" rel="noopener noreferrer">
                                <div class="instagram-wrapper">
                                    <i class="bi bi-instagram instagram-icon"></i>
                                    <span class="instagram-text">Instagram</span>
                                    <i class="bi bi-box-arrow-up-right instagram-arrow"></i>
                                </div>
                            </a>
                            <a class="nav-link telegram-bot-link" href="https://t.me/CloudCityCenter_Bot" target="_blank">
                                <div class="telegram-bot-wrapper">
                                    <i class="bi bi-telegram telegram-icon"></i>
                                    <span class="telegram-bot-text">Telegram</span>
                                    <i class="bi bi-box-arrow-up-right telegram-bot-arrow"></i>
                                </div>
                            </a>
                            <a class="nav-link whatsapp-link" href="https://wa.me/33773166380" target="_blank" rel="noopener noreferrer">
                                <div class="whatsapp-wrapper">
                                    <i class="bi bi-whatsapp whatsapp-icon"></i>
                                    <span class="whatsapp-text">WhatsApp</span>
                                    <i class="bi bi-box-arrow-up-right whatsapp-arrow"></i>
                                </div>
                            </a>
                        </div>

                    <!-- Right Navigation -->
                    <ul class="navbar-nav navbar-nav-right">
                        @{
                            var currentCulture = System.Globalization.CultureInfo.CurrentCulture.Name;
                            var cultureNames = new Dictionary<string, string>
                            {
                                { "en", "English" },
                                { "uk", "Українська" },
                                { "ru", "Русский" },
                                { "fr", "Français" },
                                { "de", "Deutsch" },
                                { "es", "Español" },
                                { "pl", "Polski" }
                            };
                            var currentLanguage = cultureNames.ContainsKey(currentCulture) ? cultureNames[currentCulture] : "English";
                        }
                        <li class="nav-item">
                            <a class="nav-link @(currentController == "About" ? "active" : "")" asp-area="" asp-controller="About" asp-action="Index">
                                <i class="bi bi-info-circle me-1"></i>@Localizer["About"]
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(currentController == "Contact" ? "active" : "")" asp-area="" asp-controller="Contact" asp-action="Index">
                                <i class="bi bi-envelope me-1"></i>@Localizer["Contact"]
                            </a>
                        </li>
                        <!-- Language Switcher -->
                        <li class="nav-item dropdown language-switcher d-none d-lg-block">
                            <a class="nav-link language-switcher-btn" href="#" id="languageDropdown" role="button" data-bs-toggle="dropdown" data-bs-auto-close="outside" aria-expanded="false">
                                <i class="bi bi-globe"></i>
                                <span class="language-text">@currentLanguage</span>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end language-dropdown" aria-labelledby="languageDropdown">
                                @foreach (var lang in cultureNames)
                                {
                                    <li>
                                        <form asp-controller="Language" asp-action="SetLanguage" method="post" class="language-form">
                                            @Html.AntiForgeryToken()
                                            <input type="hidden" name="culture" value="@lang.Key" />
                                            <input type="hidden" name="returnUrl" value="@Context.Request.Path@Context.Request.QueryString" />
                                            <button type="submit" class="dropdown-item language-option @(currentCulture == lang.Key ? "active" : "")" data-language-code="@lang.Key">
                                                <span class="language-name">@lang.Value</span>
                                                @if (currentCulture == lang.Key)
                                                {
                                                    <i class="bi bi-check-lg ms-auto text-primary"></i>
                                                }
                                            </button>
                                        </form>
                                    </li>
                                }
                            </ul>
                        </li>
                        <li class="nav-item position-relative">
                            <a class="nav-link cart-link" asp-controller="Cart" asp-action="Index">
                                <i class="bi bi-cart3 cart-icon-large"></i>
                                <span class="cart-label">@Localizer["Cart"]</span>
                                <span id="cart-count" class="cart-badge" @(cartCount == 0 ? "style=\"display:none;\"" : "")>@cartCount</span>
                            </a>
                        </li>
                        @if (User.Identity?.IsAuthenticated ?? false)
                        {
                            <li class="nav-item">
                                <a class="nav-link @(currentController == "Account" ? "active" : "")" asp-area="" asp-controller="Account" asp-action="Profile">
                                    <i class="bi bi-person-circle me-1"></i>@Localizer["Profile"]
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link @(currentController == "Orders" ? "active" : "")" asp-area="" asp-controller="Orders" asp-action="Index">
                                    <i class="bi bi-receipt me-1"></i>@Localizer["Orders"]
                                </a>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item">
                                <a class="nav-link login-link @(currentController == "Account" && ViewContext.RouteData.Values["action"]?.ToString() == "Login" ? "active" : "")" asp-controller="Account" asp-action="Login">
                                    <i class="bi bi-box-arrow-in-right me-1"></i>@Localizer["Login"]
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link register-link @(currentController == "Account" ? "active" : "")" asp-controller="Account" asp-action="Register">
                                    <i class="bi bi-person-plus me-1"></i>@Localizer["Register"]
                                </a>
                            </li>
                        }
                        @if (User.IsInRole("Admin"))
                        {
                            <li class="nav-item">
                                <a class="nav-link admin-link" asp-area="Admin" asp-controller="Products" asp-action="Index">
                                    <i class="bi bi-shield-check me-1"></i>Admin
                                </a>
                            </li>
                        }
                    </ul>
                    </div>
                </div>
            </div>
        </nav>
        
        <!-- Mobile Offcanvas Menu -->
        <div class="offcanvas offcanvas-end" tabindex="-1" id="mobileMenu" aria-labelledby="mobileMenuLabel">
            <div class="offcanvas-header border-bottom">
                <div class="offcanvas-header-content d-flex align-items-center w-100 gap-2">
                    <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index" data-offcanvas-close>
                        <img src="~/images/logo.png" alt="CloudCityCenter Logo" class="navbar-logo" style="height: 32px;">
                    </a>
                    <div class="d-flex align-items-center justify-content-center gap-2">
                        <a class="nav-link instagram-link-mobile" href="https://instagram.com/cloudcity.center" target="_blank" rel="noopener noreferrer">
                            <div class="instagram-wrapper-mobile">
                                <i class="bi bi-instagram instagram-icon-mobile"></i>
                            </div>
                        </a>
                        <a class="nav-link telegram-bot-link-offcanvas" href="https://t.me/CloudCityCenter_Bot" target="_blank">
                            <div class="telegram-bot-wrapper-mobile">
                                <i class="bi bi-telegram telegram-icon-mobile"></i>
                            </div>
                        </a>
                        <a class="nav-link whatsapp-link-mobile" href="https://wa.me/33773166380" target="_blank" rel="noopener noreferrer">
                            <div class="whatsapp-wrapper-mobile">
                                <i class="bi bi-whatsapp whatsapp-icon-mobile"></i>
                            </div>
                        </a>
                    </div>
                    <a class="nav-link cart-link-offcanvas position-relative" asp-controller="Cart" asp-action="Index" data-offcanvas-close>
                        <i class="bi bi-cart3"></i>
                        <span id="cart-count-offcanvas" class="cart-badge-mobile" @(cartCount == 0 ? "style=\"display:none;\"" : "")>@cartCount</span>
                    </a>
                    <button type="button" class="btn-close ms-auto" data-bs-dismiss="offcanvas" aria-label="Close"></button>
                </div>
            </div>
            <div class="offcanvas-body p-0">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link @(currentController == "Home" ? "active" : "")" asp-area="" asp-controller="Home" asp-action="Index" data-offcanvas-close>
                            <i class="bi bi-house me-2"></i>@Localizer["Home"]
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @(currentController == "About" ? "active" : "")" asp-area="" asp-controller="About" asp-action="Index" data-offcanvas-close>
                            <i class="bi bi-info-circle me-2"></i>@Localizer["About"]
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @(currentController == "Services" ? "active" : "")" asp-area="" asp-controller="Services" asp-action="Index" data-offcanvas-close>
                            <i class="bi bi-gear me-2"></i>@Localizer["Services"]
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="https://instagram.com/cloudcity.center" target="_blank" rel="noopener noreferrer" data-offcanvas-close>
                            <i class="bi bi-instagram me-2"></i>Instagram
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @(currentController == "Servers" ? "active" : "")" asp-area="" asp-controller="Servers" asp-action="Index" data-offcanvas-close>
                            <i class="bi bi-server me-2"></i>@Localizer["Servers"]
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @(currentController == "Hosting" ? "active" : "")" asp-area="" asp-controller="Hosting" asp-action="Index" data-offcanvas-close>
                            <i class="bi bi-globe me-2"></i>@Localizer["Hosting"]
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @(string.Equals(currentController, "VPN", StringComparison.OrdinalIgnoreCase) ? "active" : "")" asp-area="" asp-controller="Vpn" asp-action="Index" data-offcanvas-close>
                            <i class="bi bi-shield-lock me-2"></i>@Localizer["VPN"]
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @(string.Equals(currentController, "VPS", StringComparison.OrdinalIgnoreCase) ? "active" : "")" asp-area="" asp-controller="Vps" asp-action="Index" data-offcanvas-close>
                            <i class="bi bi-hdd-stack me-2"></i>@Localizer["VPS"]
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @(string.Equals(currentController, "VDI", StringComparison.OrdinalIgnoreCase) ? "active" : "")" asp-area="" asp-controller="VDI" asp-action="Index" data-offcanvas-close>
                            <i class="bi bi-display me-2"></i>@Localizer["VDI"]
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link @(currentController == "Contact" ? "active" : "")" asp-area="" asp-controller="Contact" asp-action="Index" data-offcanvas-close>
                            <i class="bi bi-envelope me-2"></i>@Localizer["Contact"]
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link cart-link-mobile-menu d-flex align-items-center justify-content-between" asp-controller="Cart" asp-action="Index" data-offcanvas-close>
                            <span class="d-flex align-items-center">
                                <i class="bi bi-cart3 me-2"></i>@Localizer["Cart"]
                            </span>
                            <span id="cart-count-mobile" class="cart-badge-inline" @(cartCount == 0 ? "style=\"display:none;\"" : "")>@cartCount</span>
                        </a>
                    </li>
                    @if (User.Identity?.IsAuthenticated ?? false)
                    {
                        <li class="nav-item">
                            <a class="nav-link @(currentController == "Account" ? "active" : "")" asp-area="" asp-controller="Account" asp-action="Profile" data-offcanvas-close>
                                <i class="bi bi-person-circle me-2"></i>@Localizer["Profile"]
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(currentController == "Orders" ? "active" : "")" asp-area="" asp-controller="Orders" asp-action="Index" data-offcanvas-close>
                                <i class="bi bi-receipt me-2"></i>@Localizer["Orders"]
                            </a>
                        </li>
                    }
                    else
                    {
                        <li class="nav-item">
                            <a class="nav-link @(currentController == "Account" && ViewContext.RouteData.Values["action"]?.ToString() == "Login" ? "active" : "")" asp-controller="Account" asp-action="Login" data-offcanvas-close>
                                <i class="bi bi-box-arrow-in-right me-2"></i>@Localizer["Login"]
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link @(currentController == "Account" ? "active" : "")" asp-controller="Account" asp-action="Register" data-offcanvas-close>
                                <i class="bi bi-person-plus me-2"></i>@Localizer["Register"]
                            </a>
                        </li>
                    }
                    @if (User.IsInRole("Admin"))
                    {
                        <li class="nav-item">
                            <a class="nav-link admin-link" asp-area="Admin" asp-controller="Products" asp-action="Index" data-offcanvas-close>
                                <i class="bi bi-shield-check me-2"></i>Admin
                            </a>
                        </li>
                    }
                    <li class="nav-item mt-auto pt-3 border-top">
                        <div class="language-mobile-header px-3 py-2 mb-2" style="font-weight: 600; color: #0d6efd; cursor: pointer; display: flex; align-items: center; justify-content: space-between;">
                            <div style="display: flex; align-items: center;">
                                <i class="bi bi-globe me-2"></i>@Localizer["Language"]
                            </div>
                            <i class="bi bi-chevron-down language-chevron" style="transition: transform 0.3s ease;"></i>
                        </div>
                        <div class="language-mobile-list" style="display: none;">
                            @foreach (var lang in cultureNames)
                            {
                                <form asp-controller="Language" asp-action="SetLanguage" method="post" class="language-form-mobile">
                                    @Html.AntiForgeryToken()
                                    <input type="hidden" name="culture" value="@lang.Key" />
                                    <input type="hidden" name="returnUrl" value="@Context.Request.Path@Context.Request.QueryString" />
                                    <button type="submit" class="nav-link language-mobile-item @(currentCulture == lang.Key ? "active" : "")" style="padding-left: 40px !important;">
                                        <span class="language-name">@lang.Value</span>
                                        @if (currentCulture == lang.Key)
                                        {
                                            <i class="bi bi-check-lg ms-auto text-primary"></i>
                                        }
                                    </button>
                                </form>
                            }
                        </div>
                    </li>
                </ul>
            </div>
        </div>
    </header>
    @{
    var isContactPage = Context.Request.Path.Value?.ToLower().Contains("/contact") ?? false;
}

@if (isContactPage)
{
    <main role="main" class="pb-3">
        @RenderBody()
    </main>
}
else
{
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>
}


    @await Html.PartialAsync("_Footer")
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
    <!-- CRITICAL: Force mobile menu button to be visible immediately -->
    <script>
      (function() {
        function forceButtonVisible() {
          // Check the button
          var btn = document.getElementById('mobileMenuBtn');
          
          var container = document.querySelector('.mobile-nav-right') ||
                          document.querySelector('div.mobile-nav-right') ||
                          document.querySelector('div.d-flex.mobile-nav-right');
          
          var testBtn = document.getElementById('testMenuBtn');
          
          console.log('=== FORCING BUTTON VISIBLE ===');
          console.log('Test button found:', testBtn ? 'YES' : 'NO');
          console.log('Main button found:', btn ? 'YES' : 'NO');
          console.log('Container found:', container ? 'YES' : 'NO');
          
          // Check if test button is visible
          if (testBtn) {
            var testDisplay = window.getComputedStyle(testBtn).display;
            console.log('Test button display:', testDisplay);
            if (testDisplay === 'none') {
              testBtn.style.display = 'block';
              console.log('Test button was hidden, forcing visible');
            }
          }
          
          if (btn) {
            // Remove all classes that might hide it
            btn.classList.remove('d-none', 'd-lg-block');
            btn.classList.add('d-flex', 'd-lg-none');
            
            // Force styles with setProperty for !important - MATCH HTML INLINE STYLES
            btn.style.setProperty('display', 'flex', 'important');
            btn.style.setProperty('visibility', 'visible', 'important');
            btn.style.setProperty('opacity', '1', 'important');
            btn.style.setProperty('pointer-events', 'auto', 'important');
            btn.style.setProperty('z-index', '99999', 'important');
            
            // Force icon to be visible
            var icon = btn.querySelector('.navbar-toggler-icon');
            if (icon) {
              icon.style.setProperty('display', 'block', 'important');
              icon.style.setProperty('width', '20px', 'important');
              icon.style.setProperty('height', '20px', 'important');
              icon.style.setProperty('visibility', 'visible', 'important');
              icon.style.setProperty('opacity', '1', 'important');
            }
            
            console.log('✅ Mobile menu button FORCED to be visible');
            console.log('Button computed display:', window.getComputedStyle(btn).display);
            console.log('Button computed visibility:', window.getComputedStyle(btn).visibility);
            console.log('Button offsetWidth:', btn.offsetWidth);
            console.log('Button offsetHeight:', btn.offsetHeight);
          } else {
            console.error('❌ Mobile menu button NOT FOUND in DOM!');
            console.error('Searching for button in:', document.querySelector('.mobile-nav-right'));
            console.error('All buttons on page:', document.querySelectorAll('button').length);
          }
          
          if (container) {
            // Remove all classes that might hide it
            container.classList.remove('d-none');
            container.classList.add('d-flex', 'd-lg-none');
            
            // Force styles with setProperty
            container.style.setProperty('display', 'flex', 'important');
            container.style.setProperty('visibility', 'visible', 'important');
            container.style.setProperty('opacity', '1', 'important');
            container.style.setProperty('position', 'relative', 'important');
            container.style.setProperty('z-index', '9998', 'important');
            container.style.setProperty('flex', '0 0 auto', 'important');
            container.style.setProperty('margin-left', 'auto', 'important');
            container.style.setProperty('width', 'auto', 'important');
            container.style.setProperty('height', 'auto', 'important');
            
            console.log('✅ Mobile menu container FORCED to be visible');
            console.log('Container computed display:', window.getComputedStyle(container).display);
            console.log('Container offsetWidth:', container.offsetWidth);
            console.log('Container offsetHeight:', container.offsetHeight);
          } else {
            console.error('❌ Mobile menu container NOT FOUND in DOM!');
          }
        }
        
        // Run immediately - before anything else
        if (document.body) {
          forceButtonVisible();
        }
        
        // Run after DOM loads
        if (document.readyState === 'loading') {
          document.addEventListener('DOMContentLoaded', function() {
            forceButtonVisible();
            setTimeout(forceButtonVisible, 50);
          });
        } else {
          forceButtonVisible();
        }
        
        // Run after everything loads
        window.addEventListener('load', function() {
          forceButtonVisible();
          setTimeout(forceButtonVisible, 100);
        });
        
        // Run multiple times to catch any late rendering
        setTimeout(forceButtonVisible, 50);
        setTimeout(forceButtonVisible, 100);
        setTimeout(forceButtonVisible, 200);
        setTimeout(forceButtonVisible, 500);
        setTimeout(forceButtonVisible, 1000);
        setTimeout(forceButtonVisible, 2000);
      })();
    </script>
    @inject Microsoft.Extensions.Localization.IStringLocalizerFactory LocalizerFactory
    @{
        var cartLocalizer = LocalizerFactory.Create("Views.Cart.Index", "CloudCityCenter");
    }
    <script>
        // Передаем локализованные строки в JavaScript
        window.cartLocalization = {
            adding: '@Html.Raw(cartLocalizer["Adding"].Value.Replace("'", "\\'"))',
            added: '@Html.Raw(cartLocalizer["Added"].Value.Replace("'", "\\'"))',
            productAddedToCart: '@Html.Raw(cartLocalizer["ProductAddedToCart"].Value.Replace("'", "\\'"))',
            errorAddingToCart: '@Html.Raw(cartLocalizer["ErrorAddingToCart"].Value.Replace("'", "\\'"))',
            errorOccurred: '@Html.Raw(cartLocalizer["ErrorOccurred"].Value.Replace("'", "\\'"))'
        };
    </script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    
    <!-- Language Dropdown Fix -->
    <script>
        (function() {
            function setupLanguageDropdown() {
                var btn = document.getElementById('languageDropdown');
                if (!btn) {
                    setTimeout(setupLanguageDropdown, 100);
                    return;
                }
                
                var parent = btn.closest('.dropdown');
                if (!parent) return;
                
                var menu = parent.querySelector('.dropdown-menu') || parent.querySelector('.language-dropdown');
                if (!menu) return;
                
                // Remove Bootstrap attributes
                btn.removeAttribute('data-bs-toggle');
                btn.removeAttribute('data-bs-auto-close');
                
                // Ensure hidden initially
                menu.style.cssText = 'display: none !important; visibility: hidden !important; opacity: 0 !important;';
                
                // Toggle function
                btn.onclick = function(e) {
                    e.preventDefault();
                    e.stopPropagation();
                    
                    var isOpen = parent.classList.contains('show');
                    
                    // Close others
                    document.querySelectorAll('.dropdown.show').forEach(function(dd) {
                        if (dd !== parent) {
                            dd.classList.remove('show');
                            var m = dd.querySelector('.dropdown-menu');
                            if (m) m.style.cssText = 'display: none !important;';
                        }
                    });
                    
                    if (isOpen) {
                        parent.classList.remove('show');
                        menu.style.cssText = 'display: none !important; visibility: hidden !important; opacity: 0 !important;';
                        btn.setAttribute('aria-expanded', 'false');
                    } else {
                        parent.classList.add('show');
                        
                        // Calculate position for fixed menu - position it below the button
                        var rect = btn.getBoundingClientRect();
                        var rightPosition = window.innerWidth - rect.right;
                        
                        // Position menu below button, aligned to right edge
                        menu.style.cssText = 'display: block !important; visibility: visible !important; opacity: 1 !important; position: fixed !important; top: ' + (rect.bottom + 2) + 'px !important; right: ' + rightPosition + 'px !important; left: auto !important; z-index: 9999 !important; min-width: 180px !important;';
                        
                        btn.setAttribute('aria-expanded', 'true');
                    }
                };
                
                // Close on outside click
                document.addEventListener('click', function(e) {
                    if (!parent.contains(e.target)) {
                        parent.classList.remove('show');
                        menu.style.cssText = 'display: none !important; visibility: hidden !important; opacity: 0 !important;';
                        btn.setAttribute('aria-expanded', 'false');
                    }
                });
            }
            
            // Run after page load
            if (document.readyState === 'loading') {
                document.addEventListener('DOMContentLoaded', setupLanguageDropdown);
            } else {
                setupLanguageDropdown();
            }
            setTimeout(setupLanguageDropdown, 500);
        })();
    </script>
    
    @await RenderSectionAsync("Scripts", required: false)


<button id="scrollTopBtn" title="Go to top">↑</button>

<script>
(function(){
  const viewport = document.querySelector('.services-viewport');
  const track = document.querySelector('.services-track');
  if(!viewport || !track) return;

  // Если дубликата нет — создаём
  if (track.dataset.cloned !== '1') {
    track.innerHTML = track.innerHTML + track.innerHTML; // дублируем набор А -> B
    track.dataset.cloned = '1';
  }

  function recalc(){
    const items = Array.from(track.children);
    const half = items.length / 2;              // первая половина — исходный набор А
    const gap = parseFloat(getComputedStyle(track).gap || '0');
    let width = 0;

    for (let i = 0; i < half; i++){
      width += items[i].getBoundingClientRect().width;
      if (i < half - 1) width += gap;          // между карточками
    }

    // проставляем точное расстояние сдвига и длительность по пикселям/секунду
    track.style.setProperty('--loop-distance', width + 'px');
    const pxPerSec = 120;                       // скорость: пикселей в секунду
    const dur = Math.max(10, Math.round(width / pxPerSec));
    track.style.setProperty('--dur', dur + 's');
  }

  // Стартовый расчёт и пересчёт при ресайзе
  recalc();
  let t;
  window.addEventListener('resize', () => {
    clearTimeout(t);
    t = setTimeout(recalc, 120);
  });
})();
</script>

<script>
  // Функция для переключения меню языков
  function toggleLanguageMenu(event) {
    if (event) {
      event.preventDefault();
      event.stopPropagation();
    }
    const header = event ? event.currentTarget : event;
    const switcher = header.closest('.language-switcher-mobile');
    const chevron = header.querySelector('.language-chevron');
    if (switcher) {
      switcher.classList.toggle('active');
      if (chevron) {
        chevron.classList.toggle('bi-chevron-down');
        chevron.classList.toggle('bi-chevron-up');
      }
    }
    return false;
  }
  
  // Инициализация аккордеона языков при загрузке
  document.addEventListener('DOMContentLoaded', function() {
    const languageHeaders = document.querySelectorAll('.language-mobile-header');
    languageHeaders.forEach(function(header) {
      header.addEventListener('click', function(e) {
        e.preventDefault();
        e.stopPropagation();
        const list = header.nextElementSibling;
        const chevron = header.querySelector('.language-chevron');
        
        if (list) {
          const isHidden = list.style.display === 'none' || !list.style.display;
          list.style.display = isHidden ? 'block' : 'none';
          
          if (chevron) {
            if (isHidden) {
              chevron.classList.remove('bi-chevron-down');
              chevron.classList.add('bi-chevron-up');
            } else {
              chevron.classList.remove('bi-chevron-up');
              chevron.classList.add('bi-chevron-down');
            }
          }
        }
      }, true);
    });
  });
  
  // Обработка переключения языков в мобильной версии (offcanvas)
  (function() {
    function initLanguageSwitcher() {
      const mobileMenu = document.querySelector('#mobileMenu');
      const languageFormsMobile = document.querySelectorAll('form.language-form-mobile');
      
      if (!mobileMenu || languageFormsMobile.length === 0) return;
      
      // Обрабатываем клики на кнопки переключения языков в мобильной версии
      languageFormsMobile.forEach(function(form) {
        const submitButton = form.querySelector('button[type="submit"]');
        
        if (submitButton) {
          submitButton.addEventListener('click', function(e) {
            // Останавливаем всплытие, но не предотвращаем submit
            e.stopPropagation();
            
            // Закрываем offcanvas меню после небольшой задержки
            if (mobileMenu && typeof bootstrap !== 'undefined' && bootstrap.Offcanvas) {
              setTimeout(function() {
                const bsOffcanvas = bootstrap.Offcanvas.getInstance(mobileMenu);
                if (bsOffcanvas) {
                  bsOffcanvas.hide();
                }
              }, 100);
            }
          }, true);
        }
      });
    }
    
    // Инициализируем после полной загрузки
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initLanguageSwitcher);
    } else {
      setTimeout(initLanguageSwitcher, 100);
    }
  })();
  
  // Mobile menu - Bootstrap handles toggle automatically
  // This just handles close buttons and aria attributes
  (function() {
    function initMobileMenu() {
      var menu = document.getElementById('mobileMenu');
      var btn = document.getElementById('mobileMenuBtn');
      var container = document.querySelector('.mobile-nav-right');
      
      // Debug: Check if elements exist
      console.log('=== Mobile Menu Debug ===');
      console.log('Menu element:', menu ? 'Found' : 'NOT FOUND');
      console.log('Button element:', btn ? 'Found' : 'NOT FOUND');
      console.log('Container element:', container ? 'Found' : 'NOT FOUND');
      
      if (container) {
        console.log('Container display:', window.getComputedStyle(container).display);
        console.log('Container visibility:', window.getComputedStyle(container).visibility);
        console.log('Container opacity:', window.getComputedStyle(container).opacity);
      }
      
      if (btn) {
        console.log('Button display:', window.getComputedStyle(btn).display);
        console.log('Button visibility:', window.getComputedStyle(btn).visibility);
        console.log('Button computed styles:', window.getComputedStyle(btn));
      }
      
      if (!menu || !btn) {
        console.log('Waiting for elements...');
        setTimeout(initMobileMenu, 100);
        return;
      }
      
      // Verify Bootstrap is loaded
      if (typeof bootstrap === 'undefined') {
        console.error('Bootstrap is not loaded!');
        setTimeout(initMobileMenu, 200);
        return;
      }
      
      console.log('Mobile menu elements found, Bootstrap available');
      var offcanvasInstance = bootstrap.Offcanvas.getOrCreateInstance(menu, {
        backdrop: true,
        scroll: false
      });
      
      // Force button to be visible - AGGRESSIVE
      btn.style.cssText = 'display: flex !important; visibility: visible !important; opacity: 1 !important; pointer-events: auto !important; z-index: 1051 !important;';
      
      if (container) {
        container.style.cssText = 'display: flex !important; visibility: visible !important; opacity: 1 !important; position: relative !important; z-index: 1050 !important; flex: 0 0 auto !important;';
        console.log('Container styles forced');
      }
      
      // Also check parent
      var parent = btn.parentElement;
      if (parent) {
        parent.style.cssText = 'display: flex !important; visibility: visible !important; opacity: 1 !important;';
        console.log('Parent container styles forced');
      }
      
      console.log('Button forced to be visible');
      
      // Handle close buttons with data-offcanvas-close attribute
      var closeLinks = menu.querySelectorAll('a[data-offcanvas-close]');
      closeLinks.forEach(function(link) {
        link.addEventListener('click', function(e) {
          e.preventDefault();
          if (bootstrap.Offcanvas) {
            var offcanvasInstance = bootstrap.Offcanvas.getInstance(menu);
            if (offcanvasInstance) {
              offcanvasInstance.hide();
            }
          }
        });
      });
      
      // Update button aria-expanded when menu opens/closes
      menu.addEventListener('show.bs.offcanvas', function() {
        btn.setAttribute('aria-expanded', 'true');
        console.log('Menu opened');
      });
      menu.addEventListener('hide.bs.offcanvas', function() {
        btn.setAttribute('aria-expanded', 'false');
        console.log('Menu closed');
      });
      
      // Test button click
      if (!btn.dataset.offcanvasBound) {
        btn.addEventListener('click', function(e) {
          e.preventDefault();
          e.stopPropagation();
          console.log('Menu button was clicked!');
          offcanvasInstance.toggle();
        });
        btn.dataset.offcanvasBound = 'true';
      }
      
      console.log('Mobile menu initialized successfully');
    }
    
    // Initialize after DOM and Bootstrap are ready
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', function() {
        setTimeout(initMobileMenu, 300);
      });
    } else {
      setTimeout(initMobileMenu, 300);
    }
    
    // Also try after window loads
    window.addEventListener('load', function() {
      setTimeout(initMobileMenu, 500);
    });
  })();
</script>

    <!-- FINAL FIX: Create button dynamically - GUARANTEED TO WORK -->
    <script>
      // Кнопка уже есть в HTML, не нужно создавать её через JavaScript
      document.addEventListener('DOMContentLoaded', function() {
        var btn = document.getElementById('mobileMenuBtn');
        if (btn) {
          console.log('✅ Кнопка меню найдена и готова к работе');
        } else {
          console.error('❌ Кнопка меню НЕ НАЙДЕНА!');
        }
      });
    </script>

</body>
</html>
