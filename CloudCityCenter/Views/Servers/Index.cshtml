@model IEnumerable<CloudCityCenter.Models.ViewModels.ProductCardVm>
@{
    ViewData["Title"] = "Dedicated Servers";
}

<section class="container py-4">
    <h1 class="mb-3">Dedicated Servers</h1>
    <p class="text-muted">
        Our bare‑metal servers deliver uncompromised performance with full hardware control.
        Ideal for large workloads, game hosting or any application that demands raw power.
    </p>

    <h5>What you get</h5>
    <ul>
        <li>Enterprise hardware & root access</li>
        <li>Multiple datacenter locations</li>
        <li>24/7 monitoring & support</li>
    </ul>
</section>

<section class="container my-5">
    <div class="row row-cols-1 row-cols-md-3 g-4">
        @foreach (var item in Model)
        {
            <div class="col">
                <div class="card h-100">
                    @await Html.PartialAsync("_ProductCard", item)
                </div>
            </div>
        }
    </div>
</section>

