@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model CloudCityCenter.Models.Server

@{
    ViewData["Title"] = "Details";
    Layout = "_Layout";
}

<h1>Details</h1>

<div>
    <h4>Server</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Id</dt>
        <dd class="col-sm-10">@Model.Id</dd>
        <dt class="col-sm-2">Name</dt>
        <dd class="col-sm-10">@Model.Name</dd>
        <dt class="col-sm-2">Slug</dt>
        <dd class="col-sm-10">@Model.Slug</dd>
        <dt class="col-sm-2">Description</dt>
        <dd class="col-sm-10">@Model.Description</dd>
        <dt class="col-sm-2">Location</dt>
        <dd class="col-sm-10">@Model.Location</dd>
        <dt class="col-sm-2">Price</dt>
        <dd class="col-sm-10">@Model.PricePerMonth</dd>
        <dt class="col-sm-2">CPU</dt>
        <dd class="col-sm-10">@Model.CPU</dd>
        <dt class="col-sm-2">RamGb</dt>
        <dd class="col-sm-10">@Model.RamGb</dd>
        <dt class="col-sm-2">StorageGb</dt>
        <dd class="col-sm-10">@Model.StorageGb</dd>
        <dt class="col-sm-2">ImageUrl</dt>
        <dd class="col-sm-10">@Model.ImageUrl</dd>
        <dt class="col-sm-2">IsActive</dt>
        <dd class="col-sm-10">@Model.IsActive</dd>
        <dt class="col-sm-2">DDoSTier</dt>
        <dd class="col-sm-10">@Model.DDoSTier</dd>
        <dt class="col-sm-2">Stock</dt>
        <dd class="col-sm-10">@Model.Stock</dd>
        <dt class="col-sm-2">CreatedUtc</dt>
        <dd class="col-sm-10">@Model.CreatedUtc</dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">Edit</a>
    <a asp-action="Index" class="btn btn-secondary">Back to List</a>
</div>
