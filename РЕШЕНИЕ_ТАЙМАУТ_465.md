# 🔧 Решение таймаута на порту 465

## Проблема

```
Connection to smtp.hostinger.com:465 timed out after 30 seconds
```

Подключение к порту 465 таймаутит, хотя Hostinger разрешает внешние SMTP подключения.

## Решение 1: Попробовать порт 587 (StartTLS)

Порт 587 часто работает лучше, чем 465. Измените настройки:

```bash
sudo systemctl edit cloudcity --full
```

Измените:
```ini
Environment=Email__SmtpPort=587
Environment=Email__UseSsl=true
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl restart cloudcity
```

## Решение 2: Проверка файрвола

Проверьте, не блокирует ли файрвол порт 465:

```bash
# Проверьте статус файрвола
sudo ufw status

# Если файрвол активен, разрешите порты
sudo ufw allow 465/tcp
sudo ufw allow 587/tcp

# Или временно отключите для теста
sudo ufw disable
# (после теста включите обратно: sudo ufw enable)
```

## Решение 3: Тест подключения с сервера

Проверьте доступность SMTP сервера напрямую:

```bash
# Тест порта 465
timeout 10 telnet smtp.hostinger.com 465

# Тест порта 587
timeout 10 telnet smtp.hostinger.com 587

# Тест через openssl (порт 465)
timeout 10 openssl s_client -connect smtp.hostinger.com:465 -quiet

# Тест через openssl (порт 587)
timeout 10 openssl s_client -connect smtp.hostinger.com:587 -starttls smtp
```

Если все таймаутят - проблема в сети или файрволе.

## Решение 4: Проверка DNS и сети

```bash
# Проверка DNS
nslookup smtp.hostinger.com
dig smtp.hostinger.com

# Проверка доступности
ping smtp.hostinger.com

# Проверка маршрута
traceroute smtp.hostinger.com
```

## Решение 5: Временное отключение проверки SSL

Для диагностики можно временно отключить проверку сертификата:

```bash
sudo systemctl edit cloudcity --full
```

Добавьте:
```ini
Environment=Email__CheckCertificate=false
```

⚠️ **ВНИМАНИЕ:** Только для тестирования! После диагностики верните `true`.

## Решение 6: Проверка пароля

Убедитесь, что пароль правильный и нет лишних символов:

1. Попробуйте войти в почтовый ящик через веб-интерфейс Hostinger
2. Проверьте, что пароль в файле конфигурации точно такой же
3. Если пароль содержит специальные символы, используйте кавычки:
   ```ini
   Environment=Email__SmtpPassword='пароль$с$символами'
   ```

## Рекомендуемый порядок действий

1. **Сначала попробуйте порт 587:**
   ```bash
   sudo systemctl edit cloudcity --full
   # Измените Email__SmtpPort=587
   sudo systemctl daemon-reload
   sudo systemctl restart cloudcity
   ```

2. **Проверьте файрвол:**
   ```bash
   sudo ufw allow 587/tcp
   sudo ufw allow 465/tcp
   ```

3. **Протестируйте подключение:**
   ```bash
   timeout 10 telnet smtp.hostinger.com 587
   ```

4. **Проверьте логи:**
   ```bash
   sudo journalctl -u cloudcity -f
   ```

5. **Отправьте форму и смотрите результат**

## Если ничего не помогает

1. **Свяжитесь с поддержкой Hostinger:**
   - Спросите про доступность портов 465 и 587
   - Уточните, нет ли блокировок на их стороне
   - Попросите проверить логи

2. **Проверьте настройки сети сервера:**
   - Возможно, провайдер блокирует исходящие подключения на эти порты
   - Проверьте настройки роутера/файрвола

3. **Рассмотрите альтернативу:**
   - Web3Forms (уже настроено в коде)
   - Другой SMTP сервис (SendGrid, Mailgun)

---

**Скорее всего поможет переход на порт 587!** Попробуйте сначала это.

