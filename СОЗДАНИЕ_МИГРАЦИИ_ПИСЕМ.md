# 📧 Создание миграции для таблицы писем

## Что нужно сделать

После добавления модели `ContactMessage` и `DbSet<ContactMessage>` в `ApplicationDbContext`, нужно создать и применить миграцию.

### Шаг 1: Создать миграцию

На сервере или локально выполните:

```bash
cd CloudCityCenter
dotnet ef migrations add AddContactMessages --project CloudCityCenter
```

Это создаст файл миграции в папке `Migrations/`.

### Шаг 2: Применить миграцию

**На сервере:**

```bash
# Установите строку подключения
export ConnectionStrings__DefaultConnection="Server=...;Database=...;User Id=...;Password=..."

# Примените миграцию
dotnet ef database update --project CloudCityCenter

# Перезапустите сервис
sudo systemctl restart cloudcity
```

**Или через приложение:**

Миграции применяются автоматически при запуске приложения (см. `Program.cs`), если установлена строка подключения.

### Шаг 3: Проверка

После применения миграции:

1. Откройте админ-панель: `/Admin/Messages`
2. Отправьте тестовую форму на странице Contact или About
3. Проверьте, что письмо появилось в списке

## Структура таблицы

Таблица `ContactMessages` будет содержать:
- `Id` - уникальный идентификатор
- `Name` - имя отправителя
- `Email` - email отправителя
- `Phone` - телефон (опционально)
- `Subject` - тема (опционально)
- `ServiceType` - тип услуги (опционально)
- `Message` - текст сообщения
- `SourcePage` - страница источника ("About" или "Contact")
- `CreatedAt` - дата создания
- `IsRead` - прочитано ли письмо
- `ReadAt` - дата прочтения (опционально)

## Важно

- Письма сохраняются в БД **перед** отправкой email
- Если отправка email не удалась, письмо все равно будет в БД
- Это позволяет не потерять заявки даже при проблемах с SMTP

