# 🔧 Исправление таймаута SMTP подключения

## Проблема

В логах видно:
```
Failed to connect to SMTP server smtp.hostinger.com:465. Error: The operation has timed out.
```

Подключение к порту 465 таймаутит - Hostinger не отвечает.

## Решение 1: Попробовать порт 587 (StartTLS)

Hostinger может требовать порт 587 вместо 465. Обновите настройки:

```bash
sudo nano /etc/systemd/system/cloudcity.service
```

Измените:
```ini
Environment=Email__SmtpPort=587
Environment=Email__UseSsl=true
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl restart cloudcity
```

## Решение 2: Проверить настройки Hostinger

В панели управления Hostinger:

1. **Проверьте почтовый ящик:**
   - Почтовый ящик `support@cloudcity.center` создан и активен
   - Пароль правильный

2. **Проверьте SMTP настройки:**
   - Разрешены ли **внешние SMTP подключения**?
   - Нет ли блокировки по IP адресу вашего сервера
   - Порт 465 или 587 открыт для внешних подключений

3. **Проверьте логи Hostinger:**
   - Есть ли попытки подключения в логах почтового сервера?
   - Есть ли ошибки или блокировки?

## Решение 3: Тест подключения с сервера

Проверьте доступность SMTP сервера:

```bash
# Тест порта 465
timeout 10 telnet smtp.hostinger.com 465

# Тест порта 587
timeout 10 telnet smtp.hostinger.com 587

# Тест через openssl
timeout 10 openssl s_client -connect smtp.hostinger.com:465 -quiet
timeout 10 openssl s_client -connect smtp.hostinger.com:587 -starttls smtp
```

Если все таймаутят - Hostinger блокирует подключения с вашего IP.

## Решение 4: Использовать Web3Forms (альтернатива)

Если Hostinger не позволяет внешние SMTP подключения, можно использовать Web3Forms, которое уже настроено в коде.

1. Получите API ключ на https://web3forms.com
2. Обновите `CloudCityCenter/wwwroot/js/web3forms-handler.js`:
   ```javascript
   const WEB3FORMS_ACCESS_KEY = 'ваш_ключ_из_web3forms';
   ```
3. Убедитесь, что формы имеют класс `web3form`

## Решение 5: Связаться с поддержкой Hostinger

Если ничего не помогает:

1. Соберите информацию:
   - IP адрес вашего сервера
   - Логи подключения
   - Настройки SMTP

2. Свяжитесь с поддержкой Hostinger и спросите:
   - Разрешены ли внешние SMTP подключения для вашего домена?
   - Какой порт использовать (465 или 587)?
   - Нужны ли специальные настройки?
   - Нет ли блокировки по IP адресу?

## Быстрое решение (попробовать сейчас)

1. **Измените порт на 587:**
   ```bash
   sudo systemctl edit cloudcity --full
   ```
   
   Найдите строку:
   ```ini
   Environment=Email__SmtpPort=465
   ```
   
   Измените на:
   ```ini
   Environment=Email__SmtpPort=587
   ```
   
   Сохраните и:
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl restart cloudcity
   ```

2. **Проверьте логи:**
   ```bash
   sudo journalctl -u cloudcity -f
   ```
   
   Отправьте форму и смотрите, что появляется.

3. **Если все еще таймаут:**
   - Проверьте настройки в панели Hostinger
   - Попробуйте Web3Forms
   - Свяжитесь с поддержкой Hostinger

---

**Скорее всего проблема в том, что Hostinger блокирует внешние SMTP подключения или требует порт 587 вместо 465.**

