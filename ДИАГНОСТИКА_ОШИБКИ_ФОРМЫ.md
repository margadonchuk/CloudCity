# 🔍 Диагностика ошибки формы

## Проблема

Форма показывает ошибку: "Произошла ошибка при отправке сообщения. Пожалуйста, попробуйте позже."

## Что проверить

### 1. Проверьте логи на сервере:

```bash
sudo journalctl -u cloudcity -n 100 | grep -i "contact\|email\|smtp\|error"
```

Ищите:
- Ошибки подключения к SMTP
- Ошибки аутентификации
- Ошибки отправки
- Детальные сообщения об ошибках

### 2. Проверьте, какая форма используется:

**Если форма использует Web3Forms:**
- Проверьте, что API ключ установлен в `web3forms-handler.js`
- Откройте консоль браузера (F12) и проверьте ошибки JavaScript

**Если форма использует серверную обработку (SMTP):**
- Проверьте логи приложения (см. выше)
- Проверьте переменные окружения

### 3. Проверьте переменные окружения:

```bash
sudo systemctl show cloudcity | grep Email
```

Убедитесь, что:
- `Email__SmtpPort=465` (двойное подчеркивание!)
- `Email__SmtpPassword` установлен
- `Email__SmtpUsername=support@cloudcity.center`

### 4. Проверьте консоль браузера:

1. Откройте сайт
2. Нажмите F12 (DevTools)
3. Перейдите на вкладку "Console"
4. Отправьте форму
5. Проверьте, есть ли ошибки JavaScript

### 5. Проверьте Network вкладку:

1. В DevTools перейдите на "Network"
2. Отправьте форму
3. Найдите запрос к `/Contact/Submit` или к Web3Forms API
4. Проверьте статус ответа и ошибки

## Возможные причины

### Причина 1: SMTP все еще не работает

Если форма использует серверную обработку:
- Проверьте логи (см. выше)
- Убедитесь, что порт 465 и пароль правильные
- Попробуйте временно отключить проверку SSL

### Причина 2: Web3Forms не настроен

Если форма использует Web3Forms:
- Проверьте, что API ключ установлен
- Проверьте консоль браузера на ошибки
- Убедитесь, что форма имеет класс `web3form`

### Причина 3: Проблема с валидацией

- Проверьте, что все обязательные поля заполнены
- Проверьте формат email
- Проверьте, что сообщение не пустое

## Быстрое решение

### Если используете SMTP:

1. Проверьте логи:
   ```bash
   sudo journalctl -u cloudcity -f
   ```

2. Отправьте форму и смотрите, что появляется в логах

3. Если видите ошибку - пришлите ее, помогу исправить

### Если используете Web3Forms:

1. Проверьте API ключ в `web3forms-handler.js`

2. Проверьте консоль браузера на ошибки

3. Убедитесь, что форма имеет класс `web3form`

---

**Пришлите логи с сервера после отправки формы - помогу разобраться!**


