# 🔧 Исправление переменных окружения

## Проблема

В файле конфигурации используется **одно подчеркивание** `Email_SmtpPassword`, а должно быть **двойное** `Email__SmtpPassword`.

В ASP.NET Core переменные окружения с двойным подчеркиванием `__` преобразуются в вложенные настройки:
- `Email__SmtpPassword` → `Email:SmtpPassword` в конфигурации
- `Email_SmtpPassword` → неправильно, не будет работать!

## Исправление

### 1. Откройте файл сервиса:

```bash
sudo systemctl edit cloudcity --full
```

### 2. Найдите все строки с `Email_` (одно подчеркивание) и замените на `Email__` (двойное):

**Было (неправильно):**
```ini
Environment=Email_SmtpHost=smtp.hostinger.com
Environment=Email_SmtpPort=587
Environment=Email_UseSsl=true
Environment=Email_SmtpUsername=support@cloudcity.center
Environment=Email_SmtpPassword=ваш_пароль
Environment=Email_RecipientEmail=support@cloudcity.center
```

**Должно быть (правильно):**
```ini
Environment=Email__SmtpHost=smtp.hostinger.com
Environment=Email__SmtpPort=587
Environment=Email__UseSsl=true
Environment=Email__SmtpUsername=support@cloudcity.center
Environment=Email__SmtpPassword=ваш_пароль
Environment=Email__RecipientEmail=support@cloudcity.center
```

**Важно:** Двойное подчеркивание `__` (два символа подчеркивания подряд)!

### 3. Сохраните файл

В nano: Ctrl+O, Enter, Ctrl+X

### 4. Примените изменения:

```bash
sudo systemctl daemon-reload
sudo systemctl restart cloudcity
```

### 5. Проверьте:

```bash
sudo systemctl show cloudcity | grep Email
```

Теперь должно быть:
```
Email__SmtpHost=smtp.hostinger.com
Email__SmtpPort=587
Email__UseSsl=true
Email__SmtpUsername=support@cloudcity.center
Email__SmtpPassword=*** (скрыто)
Email__RecipientEmail=support@cloudcity.center
```

## Если пароль содержит специальные символы

Если пароль содержит символы `$`, `&`, `!`, `%` и т.д., используйте одинарные кавычки:

```ini
Environment=Email__SmtpPassword='пароль$с$символами'
```

Или экранируйте специальные символы.

## Проверка в логах

После исправления проверьте логи:

```bash
sudo journalctl -u cloudcity -f
```

Отправьте форму и смотрите. Должно быть:
```
Email configuration: Host=smtp.hostinger.com, Port=587, Username=support@cloudcity.center, SSL=True, PasswordSet=True
```

Если `PasswordSet=False` - пароль все еще не читается правильно.

---

**Главное:** Используйте двойное подчеркивание `__` во всех переменных Email!


